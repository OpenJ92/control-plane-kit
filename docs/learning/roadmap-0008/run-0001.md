# Roadmap 0008 Run 0001 Dry-Run Learning

Date: 2026-07-16
Roadmap: Activity Execution And Runtime Mutation
Branch: `roadmap/0008-activity-execution-and-runtime-mutation`
Parent issue: #210

## Purpose

This run inspected the Roadmap 0007 implementation, Roadmap 0008 draft, ADR
0008, security policy, existing Docker/runtime code, package control routes,
Postgres stores, and the independent Pottery Factory saga package. It created
the ordered issues #211 through #227. No execution behavior was implemented.

## Existing Truth To Reuse

Roadmap 0007 provides the complete planning input:

```text
validated current/desired graphs
  -> typed GraphDiff
    -> deterministic ActivityPlan DAG
      -> durable plan
        -> approval request and decision
```

The package also already has useful scaffolding:

- `ActivityRunRecord`, `ActivityEventRecord`, and `ObservationRecord`;
- Postgres tables and append/read methods for runs, events, and observations;
- `DockerRuntimeInterpreter` plus a Docker client boundary;
- common block-control route declarations and FastAPI implementations;
- capability descriptors for health, logs, targets, switch, drain, observers,
  and restart;
- focused run/event/observation read projections; and
- ten reusable planning scenarios.

These are starting points, not finished execution contracts.

## Main Gaps

### Execution Truth Is Still String-Shaped

Run status, event type, observation status, and metadata remain open strings.
There is no execution-request record, claim owner, retry lineage, guarded
transition grammar, or typed failure evidence. Roadmap 0008 must migrate these
records deliberately rather than add parallel models.

### Postgres Stores Are Recorders, Not Execution Repositories

The current methods can append a run or event and list them. They do not claim
queued work, serialize event ordinals, enforce one active run, compare-and-set
status, or distinguish idempotent replay from conflicting intent. Those laws
belong in Postgres primitives and UnitOfWork-backed application services before
an executor exists.

### Current Docker API Is Too Broad

`DockerRuntimeInterpreter.up()` starts an entire runtime and currently reports
started nodes as healthy. `down()` operates at runtime scope. Activity execution
needs narrow node/runtime effects, inspection, explicit health probes, stable
ownership identity, retained-resource policy, and host-publication policy.
Existing recipes and client seams should be preserved while the effect surface
is refactored.

### Control Routes Exist But Need A Safe Client

Package servers expose capability, health, status, logs, targets, active-target,
drain, and observer routes. The server token is optional for development. The
Roadmap 0008 mutation client must require explicit credentials for mutation,
bound time and payloads, reject unsafe redirects, redact tokens, and verify
capabilities before calling routes. Docker/private networking is not auth.

### Saga Callbacks Are Not Durable Syntax

The Pottery Factory saga package has a clean immutable sequential/parallel
program and reverse-completion compensation interpreter. Its activities embed
Python compute and compensate callbacks. That is acceptable for an in-process
saga but cannot be persisted, reconstructed, inspected, or resumed as the
control plane's durable activity language.

The adapted shape should separate syntax from interpretation:

```text
SagaProgram[TypedEffect]
  -> EffectInterpreter[TypedEffect, EffectResult]
    -> external call
```

Callbacks may live inside an interpreter registry, not inside durable program
descriptors. Compensation remains an attempted external action, not rollback.

### ActivityPlan Order Is Not A Scheduler

`ActivityPlan` validates dependency composition and stores a deterministic
topological tuple. Execution still needs ready sets, fan-out, blocked dependents,
step outcomes, restart reconstruction, and a durable cursor. The scheduler must
remain pure and domain-neutral.

## Data-Engineering Boundary

The mandatory rhythm is:

```text
short Postgres transaction: record intent/step start
  -> commit
    -> external effect with bounded timeout
      -> short Postgres transaction: record result/event/observation
```

No transaction may remain open while Docker, HTTP, health, filesystem, cloud,
or secret-provider work executes. A crash after an effect but before result
persistence creates an explicit uncertain state. Exactly-once external effects
are not promised; idempotency keys and reconciliation are required.

## Security Boundary

Execution introduces authenticated mutation, control tokens, Docker access,
network calls, host ports, logs, image trust, retained data, and destructive
cleanup. Every child PR must state new surfaces, authorization, secret handling,
network exposure, destructive behavior, tests, and residual risk.

MCP mutation remains out of scope until the execution command services and CPI
authorization boundary are complete.

## Database Migration Boundary

Roadmap 0007 models only a cutover between pre-provisioned database endpoints.
Roadmap 0008 must require typed readiness evidence before that cutover can run.
It must not infer copy, replication, schema migration, consistency, or safe
retirement from topology. A future migration effect capability may automate
those facts; initially they remain user/provider-managed.

## Scenario Strategy

Atomic tests continue to protect codecs, transitions, claims, scheduler laws,
effect adapters, and UnitOfWork boundaries. The Roadmap 0007 scenarios are the
composed acceptance layer:

```text
PlanningScenario
  + expected ActivityRun
  + expected ActivityEvent partial order
  + expected observations
  + expected failure/compensation evidence
```

Do not replace atomic tests with scenario snapshots, and do not fork separate
scenario truth for Docker, HTTP, CPI, MCP, or UI.

## Canonical Order

```text
#211 -> #212 -> #213 -> #214 -> #215 -> #239 -> #242 -> #243
#211 + #243 -> #216 -> #217 -> #218
#218 -> #257 -> #260 -> #261 -> #258 -> #264 -> #262 -> #259 -> #263
#215 + #218 + #263 -> #244 -> #219
#218 -> #245 -> #220 and #221
#219 + #220 + #221 -> #222 -> #223 -> #224 -> #225 -> #226 -> #227
#227 -> #246 -> Roadmap 0009 lifecycle exposure
```

## Stop Conditions

Stop and request a decision if implementation would:

- hold a database transaction over an external effect;
- execute without durable approval/admission;
- rely on optional/empty control authentication for mutation;
- claim exactly-once effects without provider idempotency evidence;
- delete retained data as compensation;
- infer database migration from endpoint cutover;
- duplicate Roadmap 0007 plan or graph models;
- embed opaque callbacks in durable saga descriptors; or
- specialize generic execution around CPI, Auth, Postgres, or one example.

## Mandatory Milestone Gates

Roadmap 0008 must pause for an explicit integration review at the following
points. A gate is not merely a progress update. It is a proof that the laws
needed by the next effectful layer exist in source and tests.

Roadmap 0008 must not be executed as one uninterrupted issue chain. At every
gate, stop implementation, run the gate's full validation, update this learning
document with the source truth and review findings, and report the result before
proceeding. Later issues being planned or ready does not permit crossing a gate.

### Gate A: Durable Execution Truth (`#211` through `#215`, then `#239`,
`#242`, and `#243` hardening)

At this gate the package may admit, claim, and transition an execution attempt,
but it may not perform an external effect. Review closed execution types,
descriptor strictness, schema migration, idempotency, approval freshness,
one-winner claims, event ordinal serialization, retry lineage, and lifecycle
transition laws.

The gate is not sealed while an expired claim can be destructively cleared or
while a store exposes whole-session rewriting. Issue #242 preserves expired
lease evidence and defers recovery policy to #223. Issue #243 narrows session
mutation to guarded terminal projection transitions while append-only operation
actions retain authoritative command evidence.

### Gate B: Pure Execution Language (`#216` through `#218`)

At this gate the package has immutable saga syntax, deterministic schedule
derivation, and typed effect capabilities. Review that these modules contain no
Postgres, Docker, HTTP-client, CPI, or application-domain assumptions and that
restart reconstruction is a pure function of the plan and durable evidence.

### Gate C: Durable Fake-Effect Execution (`#244`, then `#219`)

This is the central architecture gate. Prove the complete rhythm with injected
fake effects:

```text
transaction: record step intent
  -> commit
    -> external effect
      -> transaction: record result and advance run
```

Inject crashes before the effect, after the effect, and before result commit.
The last case must become explicit uncertainty rather than blind replay. No
real Docker or HTTP interpreter may be connected before this gate passes.
Issue #244 must first establish that `current_graph_id` advances only from
durable successful execution evidence and never from an observation or a
partially completed effect prefix.

### Gate D: Real Runtime Effects (`#245`, then `#220` through `#222`)

At this gate authenticated bounded HTTP control, narrow Docker lifecycle
effects, and truthful health observations exist. Review secret handling, SSRF
and redirects, payload and timeout bounds, Docker ownership and retention,
private versus host endpoints, and the distinction between process state,
transport reachability, and application health.
Issue #245 must first make contract and derived-resource replacement staged and
failure-explicit. A build failure must preserve the old projection; cleanup
failure must become explicit uncertainty rather than hidden partial mutation.

### Gate E: Failure Semantics And Acceptance Corpus (`#223` and `#224`)

At this gate pause, resume, terminal failure, uncertainty, and compensation are
durable and visible across the reusable scenario corpus. Review reverse
completion order, compensation failure, retry admission, blocked dependents,
database readiness gates, and event partial-order assertions.

### Gate F: Live Proof And Closeout (`#225` through `#227`)

At this gate one approved graph transition executes through the real pipeline,
changes a Docker-backed router, records observations, and cleans up without
damaging retained state. Review live evidence, prior recipe compatibility,
security/data findings, public exports, and the exact Roadmap 0009 handoff.

Every gate report must state:

- the capability that now exists;
- the objects, transformations, and executable laws introduced;
- the tests and live evidence protecting those laws;
- provisional behavior and remaining risk;
- security and data-engineering findings;
- any deviation from this dry run; and
- whether the next milestone is genuinely unblocked.

## Issue-By-Issue Source Dry Run

### `#211`: Closed Execution Values And Codecs

**Existing source to migrate**

- `stores/records.py` contains open-string `ActivityRunRecord`,
  `ActivityEventRecord`, and `ObservationRecord` scaffolds.
- `read_services/instance.py`, `stores/postgres.py`, tests, and the read demo
  decode those strings directly.

**Expected implementation**

- Create an `execution` package and define closed request, run, event,
  observation, claim, retry, step-outcome, failure-category, and uncertainty
  values there.
- Define a versioned strict descriptor codec beside those values.
- Replace the three scaffold records; do not retain aliases or parallel legacy
  records.
- Keep event payloads as bounded typed evidence. Arbitrary provider response
  bodies and secret values are not part of the durable language.

**Tests and difficulty**

- Enumerate every sum variant and round-trip it through JSON.
- Reject unknown versions, variants, malformed IDs, unbounded evidence, and
  secret-bearing fields.
- The difficult decision is the minimal canonical event vocabulary. It must be
  rich enough for resume and compensation without encoding Docker/HTTP names.

**Handoff:** `#212` receives one canonical descriptor vocabulary for SQL
constraints and rows. Gate A must stop if this issue leaves open strings in
execution truth.

### `#212`: Postgres Execution Schema And Stores

**Existing source to migrate**

- `stores/postgres.py` creates append-only run/event/observation tables.
- `ActivityHistoryStore` currently combines sessions, approvals, plans, runs,
  and events into one broad interface.
- `tests/postgres_case.py` truncates the Roadmap 0007 schema and provides the
  live Postgres fixture.

**Expected implementation**

- Add execution-request, claim/lease, run lineage, step evidence, and typed
  observation columns/tables using a forward migration from the existing
  schema.
- Introduce a focused `ExecutionStore` protocol while retaining a shared
  UnitOfWork connection with history, approval, graph, and observation stores.
- Add database constraints for idempotency scope, one active attempt, event
  ordinal uniqueness, foreign workspace identity, and closed state values.
- Stores may issue SQL and flush results but must never commit.

**Tests and difficulty**

- Install the Roadmap 0007 schema, seed representative rows, migrate, and prove
  they remain readable.
- Force a late command failure and prove all participating relations roll back.
- The likely child split, if the PR grows beyond reviewable size, is
  `schema/migration` followed by `store protocols/adapters`; both remain under
  the same issue and Gate A.

**Handoff:** `#213` receives lockable rows and database invariants, not merely
append methods.

### `#213`: Claims, Ordinals, And Transition Concurrency

**Existing source to migrate**

- Run/event stores have no `FOR UPDATE`, claim primitive, compare-and-set
  transition, or ordinal allocator.
- The Roadmap 0007 action ordinal implementation is the nearest tested pattern,
  but execution claims have stronger worker-concurrency semantics.

**Expected implementation**

- Add one-winner queue claim, guarded run transition, serialized next event
  ordinal, lease expiry evidence, and explicit retry-attempt creation.
- Prefer Postgres row locking plus constraints over process locks.
- Keep policy out of stores: primitives enforce concurrency; command services
  decide whether a transition is authorized and meaningful.

**Tests and difficulty**

- Use two independent Postgres connections and real concurrent transactions.
- Prove identical claims converge, competing workers do not both win, ordinals
  remain unique, stale compare-and-set fails, and rollback releases claims.
- Do not test this with mocks; concurrency is a database property.

**Handoff:** `#214` and `#215` may rely on these primitives inside one caller-
owned UnitOfWork.

### `#214`: Execution Admission

**Existing source to extend**

- `workflows/approvals.py` owns approval request and decision truth.
- `workflows/planning.py` owns canonical persisted plans and operation evidence.
- `policies/services.py` computes approval requirements.

**Expected implementation**

- Add one transport-neutral `RequestPlanExecution` command service.
- In one UnitOfWork, load workspace/session/plan/approval/graph identity, apply
  freshness and destructive-scope policy, and persist the execution request
  with its operation action.
- Reuse the existing approval and planning records. Do not make a second
  execution-specific approval model.
- Database endpoint cutover requires a typed readiness attestation; it does not
  imply migration.

**Tests and difficulty**

- Cover missing/rejected/stale/foreign/review-blocked/insufficient-scope plans,
  idempotent replay, conflicting intent, and rollback after request insertion.
- Assert the effect registry is never called from admission tests.

**Handoff:** `#215` receives only durably admitted queue work.

### `#215`: Claimed ActivityRun Lifecycle

**Existing source to replace**

- `ActivityRunRecord` is currently created through `add_run` with no command
  grammar.
- Read projections assume only timestamps and a free-form status.

**Expected implementation**

- Define explicit claim/open, start, pause, resume, fail, compensate, complete,
  and cancel commands with guarded transition functions.
- Atomically change run truth and append the matching lifecycle event.
- Model retry as a new run linked to the prior attempt. Never reset a failed
  run or expose a generic `set_status` method.

**Tests and difficulty**

- Exhaustively test the transition matrix, idempotent duplicate commands,
  ownership mismatch, one-winner claim, event atomicity, and read projection.
- Gate A ends here. No test at this gate should create a container or make HTTP.

**Handoff:** `#219` receives a durable claimed run boundary after Gate B defines
the pure execution language.

### `#216`: Generic Saga Adaptation

**Existing source to learn from**

- `pottery-factory-saga` provides immutable `End | ActivityNode |
  ParallelNode`, `then`, `parallel`, and reverse-completion compensation.
- Its `SagaActivity` embeds compute and compensate callbacks, which cannot be
  durable control-plane syntax.

**Expected implementation**

- Implement package-local `SagaProgram[Effect]` data whose leaves contain typed
  effect descriptors and optional typed compensation descriptors.
- Add pure `SagaState`, `SagaEvent`, `evolve`, and `decide` functions.
- Keep persistence in the coordinator adapter: the saga algebra must not import
  stores, Postgres, clocks, HTTP, or Docker.

**Tests and difficulty**

- Cover sequential composition, fan-out, deterministic completion evidence,
  cancellation, failure, reverse completion compensation, and compensation
  failure as pure values.
- The old callback interpreter is a semantic reference, not a dependency to
  import unchanged.

**Handoff:** `#217` can compile plan dependencies into saga/schedule values;
`#219` later provides the durable journal adapter.

### `#217`: Resumable Dependency Scheduler

**Existing source to extend**

- `planning/activity_plan.py` validates a DAG and stores a deterministic
  topological tuple.
- Scenario expectations already declare important dependency edges.

**Expected implementation**

- Define closed step states and a pure scheduler projection from
  `(ActivityPlan, durable step evidence)` to ready, blocked, terminal, and
  compensatable sets.
- Preserve deterministic ordering inside a ready batch while allowing batches
  to fan out.
- Do not use tuple position as an execution cursor.

**Tests and difficulty**

- Cover diamonds, independent branches, failed predecessors, restart
  reconstruction, missing evidence, compensation order, and all ten scenario
  plans without domain-name branches.

**Handoff:** `#218` maps each ready typed operation to a required capability.

### `#218`: Runtime And Control Effect Protocols

**Existing source to refactor around**

- `runtimes.py` exposes whole-runtime `up/down` interpreters.
- `docker_runtime.py` defines Docker-specific plans and client methods.
- `capabilities.py` and `control_routes.py` already advertise block powers.

**Expected implementation**

- Define product-style effect request/result/error values for lifecycle,
  socket reconciliation, health, target mutation, drain, and observations.
- Define narrow interpreter protocols and a capability dispatch registry.
- Carry operation identity, idempotency identity, timeout policy, and bounded
  observation data. Carry secret references, never secret values.
- Return `unsupported` distinctly from attempted-and-failed.

**Tests and difficulty**

- A complete fake interpreter must support every safe `ActivityOperation` and
  reject unsupported operations before mutation.
- Gate B ends here with no provider implementation imported into the algebra.

**Handoff:** `#219`, `#220`, and `#221` consume this stable capability surface.

### `#219`: Durable Coordinator With Fake Effects

**Existing source to compose**

- Admission/claim/run truth comes from `#214` and `#215`.
- Saga/scheduler/effect values come from `#216` through `#218`.
- `PostgresUnitOfWork` already provides explicit caller-owned transactions.

**Expected implementation**

- Add an application coordinator that reconstructs run state, records one
  step-start event, exits the transaction, invokes one injected effect, and
  records its result in a fresh transaction.
- Adapt `ActivityEvent` persistence as the saga journal. Do not create a second
  saga database or let the saga module commit.
- Treat effect-without-result evidence as uncertain and stop for reconciliation.

**Tests and difficulty**

- Inject failure at every side of both transaction boundaries and at the effect
  boundary.
- Assert no connection/lock remains open while the fake effect executes.
- Prove replay, claim ownership, idempotent provider results, fan-out joins, and
  blocked dependents.
- This issue is intentionally a milestone-sized PR. Split internal modules and
  tests, but do not split the architecture proof across independently usable
  coordinators.

**Handoff:** Gate C must pass before concrete effects are connected.

### `#220`: Authenticated Bounded Block-Control Client

**Existing source to consume**

- `servers/block_control.py` implements common FastAPI routes and permits an
  empty development token.
- `control_routes.py` and `capabilities.py` describe route sets and powers.

**Expected implementation**

- Add an HTTP adapter over the `#218` protocols with strict base-address,
  timeout, redirect, response-size, content-type, and schema policy.
- Require a secret resolver and non-empty credential for every mutation.
- Discover and verify the advertised capability before sending a signal.
- Redact credentials and remote response bodies from errors/events.

**Tests and difficulty**

- Use real local FastAPI servers for success/auth tests and controlled malicious
  responses for redirects, oversized bodies, malformed JSON, wrong content
  type, timeout, and unsupported capability.
- Define explicit private-network address policy to prevent an arbitrary graph
  descriptor from becoming SSRF authority.

**Handoff:** `#222` can use bounded control health evidence; `#225` can switch a
real router.

### `#221`: Activity-Level Docker Effects

**Existing source to migrate**

- `DockerRuntimeInterpreter` plans/executes a whole runtime.
- `DockerCliClient.start_container()` force-removes a same-named container.
- Host ports are present in plan metadata but are not published by the CLI.
- `up()` records every started node as `healthy=True`.

**Expected implementation**

- Keep graph-to-Docker planning pure, but expose narrow ensure-network,
  start/inspect/stop/remove-node effects keyed by stable ownership labels.
- Replace destructive name collision behavior with inspect-and-verify.
- Represent owned, attached, external, ephemeral, and retained resources
  explicitly.
- Publish only declared host mappings and return private/public endpoints as
  different typed observations. Starting never asserts health.

**Tests and difficulty**

- Fake-client tests cover idempotency and ownership; live Docker tests cover
  labels, network, port publication, retained Postgres, unrelated containers,
  and cleanup.
- If review size grows, split a child hardening PR for ownership/retention and a
  second for host publication. Do not split off an alternative Docker executor.

**Handoff:** `#222` supplies actual readiness; existing runtime examples must
still run through compatibility entry points backed by the new effects.

### `#222`: Truthful Health And Observations

**Existing source to migrate**

- `ObservationRecord` is open-string evidence.
- `RuntimeNodeState.healthy` is a Boolean that conflates process and service
  health.
- Graph nodes already carry health paths and provider endpoints.

**Expected implementation**

- Define probe intent and bounded retry policy from provider socket, health
  path, and runtime address observations.
- Record process state, transport reachability, application health, timeout,
  unknown, and stale evidence separately.
- Persist the activity event and observation in one short command transaction;
  the network probe itself happens outside that transaction.

**Tests and difficulty**

- Cover started-but-unhealthy, connection refusal, timeout, malformed health,
  eventual readiness, stale evidence, private/public endpoint separation, and
  workspace ordering.
- Never mutate desired graph based on observations.

**Handoff:** Gate D ends here; `WaitForHealthy` now has truthful semantics.

### `#223`: Pause, Resume, Failure, And Compensation

**Existing source to extend**

- `planning/recovery.py` creates planning-time recovery values but is not a
  runtime retry/compensation engine.
- `#216` provides pure compensation syntax and `#219` provides durable journal
  interpretation.

**Expected implementation**

- Add typed failure classification and command services for pause, resume,
  terminal failure, uncertain effect, compensation start/result, and operator
  attention.
- Reconstruct from the journal and compensate only completed compensatable
  effects in reverse completion order.
- A changed recovery plan requires fresh admission/approval; resuming identical
  intent may reuse the original approval under explicit policy.

**Tests and difficulty**

- Cover compensation failure, non-compensatable work, unknown effect outcome,
  duplicate resume, stale claim, retryable versus terminal failure, and original
  error preservation.
- This issue may warrant a child hardening PR because recovery semantics cross
  lifecycle, saga, coordinator, and read projections.

**Handoff:** `#224` can express both success and failure acceptance contracts.

### `#224`: Execution Scenario Corpus

**Existing source to extend**

- `examples/scenarios/model.py` defines planning expectations.
- `examples/scenarios/catalog.py` defines ten transitions, including an
  explicitly blocked implementation change and a pre-provisioned database
  endpoint switch.

**Expected implementation**

- Compose `ExecutionScenarioExpectation` over `PlanningScenario`; do not alter
  planning expected output.
- Express expected run terminal state, event partial order, observations,
  blocked reason, and compensation evidence using typed values.
- Run safe scenarios through Postgres plus fake effects. Mark provider-specific
  scenarios as blocked until capability/readiness evidence exists.

**Tests and difficulty**

- Assertions target semantic partial order and typed evidence, not generated
  IDs, timestamps, or full JSON snapshots.
- The database endpoint scenario must require external migration-readiness
  evidence and must never invent migration activities.

**Handoff:** Gate E ends here; `#225` selects the router-switch scenario without
forking its truth.

### `#225`: Live Docker Router Switch

**Existing source to reuse**

- `examples/router_runtime.py`, `examples/router_swap.py`, router server blocks,
  and the backend-switch scenario already provide the teaching topology.

**Expected implementation**

- Package a live demo that admits and claims the scenario, starts or discovers
  both backends and router, verifies health, registers/switches the target, and
  records events/observations through the real coordinator.
- Make the same application request before and after the cutover and expose one
  deliberate host endpoint for human verification.
- Cleanup belongs to the demo's ownership policy and must not use broad Docker
  deletion.

**Tests and difficulty**

- Docker integration asserts response change, auth failure without token,
  evidence ordering, no effect before approval, and exact owned cleanup.
- This is proof of the generic pipeline, not a router-specific executor path.

**Handoff:** `#226` receives real logs and failure surfaces for adversarial
review.

### `#226`: Cross-Module Hardening

**Expected implementation**

- Perform code review and test-integrity classification across every Roadmap
  0008 PR.
- Add focused hardening issues/PRs for findings rather than hiding fixes in one
  unreviewable cleanup commit.
- Exercise concurrent claims, crash windows, SSRF, redaction, retained data,
  destructive approval, uncertainty, compensation failure, and adapter
  consistency.

**Acceptance evidence**

- Map every roadmap law to at least one atomic or composed test.
- Confirm no skipped/softened assertion, permissive mock, generic setter,
  provider special case, or transaction-over-effect path survives.
- Re-run all previous live deployment examples.

**Handoff:** `#227` proceeds only with no unresolved high-severity finding or an
explicit user acceptance.

### `#227`: Closeout And Roadmap 0009 Handoff

**Expected implementation**

- Update architecture, roadmap, examples, migration notes, public exports, and
  this learning record to match source truth.
- Clearly label demo adapters versus production-capable contracts.
- Prove the Roadmap 0009 prerequisite: generic machinery can start Postgres,
  start an `ApplicationBlock` with socket-derived environment, deliberately
  publish one endpoint, wait for health, and persist observations.

**Final evidence**

- Docker/Postgres test suite, live router switch, existing recipe smoke tests,
  compileall, `git diff --check`, GitHub CI, security review, data review, and
  test-integrity review all pass.
- Gate F reports residual risk and the exact unimplemented CPI packaging work.

## Dry-Run Conclusion

The canonical issue order remains correct. No issue needs to move. The roadmap
is executable if the six milestone gates are treated as stop-and-review points.
Three issues deserve likely child hardening PRs rather than larger initial
scope: `#212` (schema/store migration), `#221` (Docker ownership and host
publication), and `#223` (recovery/compensation semantics). `#219` must remain a
single architectural proof even if its source is partitioned into several
modules, because splitting it into multiple coordinators would obscure the
transaction/effect law it exists to establish.

## Gate A Review: Durable Execution Truth

Date completed: 2026-07-16
Issues completed: #211, #212, #213, #214, #215, #239
Pull requests: #229, #234, #235, #236, #237, #240

### Capability Reached

Gate A establishes durable execution truth without executing runtime effects:

```text
approved non-empty ActivityPlan
  -> guarded ExecutionRequest admission
    -> concurrency-safe worker claim
      -> admitted ActivityRun attempt
        -> typed lifecycle transition
          -> canonical ActivityEvent + operation evidence
```

The package can now admit, claim, open, retry, start, pause, resume, complete,
fail, compensate, and cancel an execution attempt. Every operator command is
one explicit Postgres transaction. No command in this layer accepts an effect
interpreter or calls Docker, HTTP, the filesystem, a cloud provider, or a
health endpoint.

An empty plan has a deliberately narrower meaning:

```text
empty GraphDiff
  -> ActivityPlan(())
    -> durable planning conclusion: no runtime work is required
      -/-> ExecutionRequest
```

An empty plan remains valid, inspectable planning truth. Admission rejects it
because creating a request, worker claim, run, or event would falsely claim
that executable work exists.

### Objects And Transformations

Gate A replaced open execution strings and recorder-style persistence with a
closed execution vocabulary and guarded application services. The central
lifecycle interpretation is:

```python
RunLifecycleCommand
  -> decide_run_transition(command, current_status)
  -> RunTransition(expected, replacement, event_kind)
  -> PostgresUnitOfWork
  -> RunLifecycleResult(request, run, event, action)
```

The durable objects now include:

- closed request, run, event, observation, failure, claim, and retry values;
- strict versioned descriptor codecs;
- execution requests with approval and intent identity;
- worker claims with leases;
- admitted run attempts with explicit retry lineage;
- canonical, ordinal `ActivityEvent` journal entries; and
- existing `OperationAction` rows as actor-attributed idempotency receipts.

`RUN_OPENED` was added to the closed event vocabulary. Reusing
`REQUEST_CLAIMED` for a retry would have made the event journal describe a
request operation that did not occur. Forward migration rebuilds the closed
Postgres check constraint to admit this constructor while continuing to reject
unknown event values.

### Executable Laws

The Docker/Postgres suite now proves:

- unknown descriptor versions, variants, lifecycle strings, and secret-shaped
  evidence fail closed;
- existing Roadmap 0007 rows migrate without fabricated admission facts;
- request claims have exactly one concurrent winner;
- event and operation ordinals serialize across real Postgres connections;
- compare-and-set run transitions reject stale state;
- one command commits request/run state, event, and operation evidence together;
- a late evidence-write failure rolls back every earlier write;
- current authorization is evaluated before idempotent replay;
- reuse of an idempotency key with different intent fails explicitly;
- retry creates a new attempt linked to the latest failed or partially failed
  run and cannot branch from an older attempt;
- failure and compensation events retain their own immutable occurrence times,
  while `settled_at` records only the final resolution of the run attempt;
- cancellation clears claim ownership and remains exactly replayable; and
- every lifecycle command accepts exactly its declared source statuses.

Integrated Gate A validation ran `git diff --check` and `./test.sh`; all 374
tests passed against Docker and real Postgres.

### Architecture Review

The implementation preserves the intended separation:

```text
planning truth -> approval -> admission -> run lifecycle
                                      |
                                      `-- no effects yet
```

The lifecycle transition table is pure data interpreted by a pure function.
The application service owns transaction entry, commit, and rollback. Stores
lock and mutate rows but never commit. No CPI, Auth, Router, Docker, Postgres
role, or scenario name specializes the execution algebra.

No competing request, run, event, planner, or receipt model was introduced.
The existing `ActivityEvent` relation remains the future saga journal, and the
existing operation history remains the user-command evidence ledger.

### Security Review

- execution admission revalidates current approval and policy-derived scope;
- lifecycle commands require a current `execution:operate` authority;
- claimed request ownership is checked before mutation;
- authorization is checked before idempotent replay, so an old receipt does not
  become a capability token;
- bounded evidence rejects secret-shaped fields; and
- Gate A introduces no network, Docker socket, token transport, redirect, SSRF,
  image, host-port, or retained-resource surface.

The worker authority remains an application-boundary value rather than a full
identity-provider integration. Roadmap 0008 must continue treating it as
verified input supplied by the future authenticated CPI command boundary, not
as a credential parser.

### Data-Engineering Review

Every command follows the Roadmap law:

```text
one operator command = one explicit Postgres transaction
```

All participating stores share the UnitOfWork connection. Request and run rows
are locked deliberately, database constraints provide final concurrency
authority, and application services perform the only commit. There is still no
transaction-over-effect path because no effect exists at this gate.

Schema installation now also acts as a forward migration for the closed event
constraint. The migration test begins with the older constraint, proves the
new constructor is accepted after installation, and proves an invented value
is still rejected.

Gate A hardening also migrates the overloaded `finished_at` projection to the
write-once `settled_at` projection. Modern failed or compensating rows migrate
only when the old timestamp is corroborated by an authoritative `RUN_FAILED`
event at the same time; otherwise installation fails closed. Final modern
statuses retain their settlement time. Genuinely legacy timestamps survive in
bounded migration metadata rather than being reinterpreted as modern truth.

### Test-Integrity Review

No test was skipped, softened, or changed merely to accept incorrect behavior.
The execution-store fixture formerly used an empty plan for convenience; it now
uses a minimal typed `StartRuntime` activity because empty plans are correctly
non-executable. The dedicated admission test retains the stronger assertion:
an empty plan is persistable planning truth, admission raises a conflict, and
no execution request is written.

The lifecycle tests use real Postgres for concurrency, row locking, rollback,
migration, idempotency, and read-projection visibility. Pure tests exhaust the
closed transition table. Mocks did not replace application behavior.

### Provisional Behavior And Residual Risk

- Claim leases are durable evidence, but Gate A does not renew leases or run a
  recovery worker. Later scheduler/coordinator work must make expired and
  uncertain ownership explicit.
- A replay returns the original command event and action together with the
  current durable run record. Read surfaces must continue distinguishing
  original command evidence from current aggregate state.
- Compensation lifecycle states now exist, but no compensation program or
  external compensating effect exists until Gates B through E.
- `FailureEvidence` records bounded operator-facing facts; provider response
  bodies and secrets must remain outside it.
- Admission proves current policy and readiness references, not the continued
  truth of an external system. Gate C and Gate D must record operational
  observations around effects.
- Database endpoint cutover remains a plan over pre-provisioned endpoints. Gate
  A provides no migration, replication, consistency, or retirement guarantee.

### Roadmap Deviations

The Gate A capability matches the dry run. Focused hardening added one event
constructor (`RUN_OPENED`), reused the operation-action ledger for lifecycle
idempotency rather than creating another receipt table, and reopened Gate A in
#239 to remove the overloaded mutable completion timestamp. The canonical
relationship is now:

```text
ActivityEvent* = authoritative append-only lifecycle history
ActivityRun.status = current projection
ActivityRun.started_at = cached first-start projection
ActivityRun.settled_at = cached write-once final-resolution projection
```

`FAILED` and `COMPENSATING` remain unsettled because later recovery policy may
continue them. `SUCCEEDED`, `COMPENSATED`, `PARTIALLY_FAILED`, and `CANCELLED`
settle the attempt. The Postgres compare-and-set boundary refuses every status
mutation after settlement. No external effect or Roadmap 0009 packaging work
was pulled forward.

### Gate A Mutation-Integrity Reopening

A post-gate mutation audit found that #239 corrected one instance of a broader
law rather than an isolated timestamp bug. Parent #241 records the complete
topology amendment:

```text
fix before Gate B: #242 -> #243
fix before Gate C: #244
fix before Gate D: #245
handoff to Roadmap 0009: #246
```

Only #242 and #243 are permitted before Gate B. The deferred issues require
semantics from later gates and must not be guessed early. Gate A is reopened
until those immediate prerequisites merge and the Docker/Postgres suite passes
again.

#### #242 Decision: Expiry Is Evidence, Not Requeue Authority

The expired-claim audit found one destructive store operation:

```text
claimed(worker-a, claimed-at, lease-expires-at)
  -> queued(worker=None, timestamps=None)
```

That operation erased the identity and timing needed to understand an already
opened run. It has been removed from the store protocol and Postgres adapter.
Lease expiry now leaves the request claimed and prevents a competing worker
from claiming it. The request remains coherent with its admitted run.

This is deliberately fail-closed. Gate A does not infer whether the worker is
dead, whether an effect happened, or whether retry is safe. Issue #223 must
introduce explicit recovery or abandonment evidence once the scheduler and
effect journal can distinguish those cases.

The strengthened concurrency test captures the original durable claim, makes
a competing claim attempt after expiry in a separate transaction, and proves
that worker identity, timestamps, request status, and run admission remain
unchanged. The full Docker/Postgres suite passes all 374 tests.

#### #243 Decision: Terminal Session Mutation Is A Guarded Projection

The activity-history store formerly accepted an entire replacement session
record and rewrote workspace, actor, title, creation time, metadata, status,
and terminal time. The command service happened to preserve those fields, but
the store contract did not express that law.

The broad operation is replaced by:

```python
transition_open_session(
    session_id,
    replacement=OperationSessionStatus.CLOSED,
    closed_at=occurred_at,
)
```

The Postgres predicate admits only an `open` source row, the adapter accepts
only `closed` or `cancelled`, and SQL updates only `status` and `closed_at`.
A stale transition returns no row and becomes an explicit
`OperationSessionStateConflict` at the application boundary.

The append-only terminal action remains in the same UnitOfWork transaction as
the projection transition. A dedicated late-write failure test proves that an
action insertion failure rolls the terminal projection back to `open`. Store
tests prove immutable session identity, ownership, title, creation time,
metadata, and idempotency evidence survive the transition exactly. The full
Docker/Postgres suite passes all 376 tests.

### Gate B Readiness

The reopened Gate A prerequisites are complete and integrated validation is
green. Gate B may consume admitted run and event types while defining pure saga
syntax, scheduler reconstruction, and effect capabilities. It must not import
Postgres stores, lifecycle command services, Docker, HTTP, CPI, or
application-domain assumptions into that pure language.

Implementation stops at the reopened Gate A milestone pending explicit
operator instruction to begin #216.

## Gate B Progress

### #216 And #250: Pure Saga Algebra And Parallel Compensation

The package now contains an immutable, callback-free saga language:

```text
SagaProgram e
  = End
  | StepNode (SagaStep e) (SagaProgram e)
  | ParallelNode [SagaProgram e] (SagaProgram e)

SagaStep e
  = SagaStepId x ForwardEffect e x OptionalCompensation e
```

`StepNode` and `ParallelNode` are structural composition. Effects and
compensations belong only to stable step leaves. This prevents a parallel
group from acquiring an ambiguous second compensation that could duplicate,
replace, or conflict with the compensations of partially completed branches.

The pure command/event projection reconstructs state from immutable facts and
derives compensation candidates from reverse durable completion order. It has
no persistence, workflow, Docker, HTTP, server, or provider dependency.

The first Docker run exposed an invalid test construction: the test completed
every branch of a terminal parallel program and then attempted cancellation.
Production correctly rejected cancellation of the already succeeded saga. The
implementation was not weakened. Hardening issue #250 replaced the impossible
fixture with two stronger scenarios:

```text
parallel(left, right) -> pending after
complete right, complete left, cancel while active
compensate left, then right
```

and:

```text
parallel(left, right) -> pending after
complete right, fail left
only right is a compensation candidate
```

The second scenario proves that failed or incomplete leaves never receive
fabricated compensation. Issue #217 must now define the separate scheduler
law: a failed branch blocks its shared continuation, while completed siblings
remain available for compensation. That rule must not be pushed backward into
`ParallelNode`.

Architecture, security, data-engineering, and test-integrity review found no
new external or persistence surface. No assertions were weakened, no test was
skipped, and no mock replaced behavior. The complete Docker/Postgres suite
passes all 385 tests.

### #217 And #252: Pure Dependency Scheduler

The scheduler preserves the two canonical input objects rather than compiling
an arbitrary DAG into misleading sequential or parallel saga syntax:

```text
ActivityPlan = validated dependency topology
SagaState = reconstructed durable step evidence

derive_schedule(ActivityPlan, SagaState) -> ExecutionSchedule
```

`ExecutionSchedule` is a pure current projection. It classifies canonical
`PlannedActivity` values as ready, running, waiting, blocked, succeeded,
failed, compensating, compensated, compensation-failed, or compensation-ready.
It does not create another durable lifecycle, mutate evidence, persist a
cursor, or perform an effect.

Readiness is dependency-local while failure and cancellation are fail-closed:

```text
all predecessors succeeded -> ready
predecessor running/pending -> waiting
predecessor failed/blocked -> explicitly blocked
review operation anywhere -> plan remains non-executable
saga failed/cancelled/compensating -> no new unrelated forward work
```

Review produced focused hardening issue #252. Schedule derivation now rejects
duplicate, missing, foreign, or internally inconsistent completion/failure
evidence. Compensation is not exposed while a forward step or another
compensation remains in flight. Cancellation therefore means "schedule no new
forward work"; it does not pretend that already-running external work vanished.
Once all in-flight work settles, compensation candidates become available in
reverse durable completion order.

The ten Roadmap 0007 planning scenarios derive deterministic initial schedules
without domain-name special cases. The unsupported implementation transition
remains explicitly review-blocked and receives no ready work.

Architecture review found no duplicate plan, saga, event, or durable scheduler
model. Security and data-engineering review found no network, secret,
persistence, transaction, or external-effect surface. Test-integrity review
found no skips, permissive mocks, or weakened lifecycle assertions. The full
Docker/Postgres suite passes all 394 tests.

### #218 Decision: Typed Provider-Neutral Effect Language

Gate B now has a closed request/result language and an interpreter protocol:

```text
EffectRequest
  = EffectIdentity
  x EffectAction
  x TimeoutPolicy
  x SecretReference*

EffectAction
  = safe ActivityOperation
  | RegisterTarget
  | ActivateTarget
  | DrainTarget
  | ObserveSubject

EffectResult
  = EffectSucceeded
  | EffectFailed
  | EffectUnsupported
```

The language reuses canonical `ActivityOperation` values instead of defining a
second lifecycle/socket vocabulary. `ReviewChange` is rejected while building
the request, before any interpreter can be called. `EffectUnsupported` is also
a preflight result: the generic dispatcher compares the action's required
typed capability with the interpreter's advertised capability set and does not
call the interpreter when the capability is absent. An attempted provider
failure is therefore structurally distinct from unsupported work.

Requests carry stable run/activity/attempt/idempotency coordinates and a
bounded timeout policy. Endpoints and secrets cross the pure boundary only as
validated opaque references; URLs, connection strings, assignments, and raw
secret values are not representable by those reference types. Evidence reuses
the existing bounded execution values rather than introducing another payload
grammar.

```python
request = effect_request_for_activity(
    activity,
    run_id="run-1",
    attempt=1,
    idempotency_key="run-1:start-api:1",
)

result = dispatch_effect(request, interpreter)
```

The dispatcher validates that an attempted result preserves the exact request
identity. Concrete HTTP, Docker, filesystem, cloud, and control-plane adapters
remain later interpreters; none is imported by `control_plane_kit.effects`.
The package-boundary test now parses Python's AST and checks actual imports,
rather than searching source text and accidentally treating docstrings as
dependencies.

Tests exhaust every safe `ActivityOperation`, every control action capability,
unsupported-before-attempt behavior, attempted failure, timeout bounds, opaque
references, identity integrity, and the Roadmap 0007 scenario corpus. Review
found no persistence or transaction surface, no provider-specialized name in
the algebra, no skipped tests, and no mock replacing application behavior. The
integrated Docker/Postgres suite passes all 407 tests before merge.

**Handoff:** Gate C issue #244 may consume typed effect outcomes to define when
evidence is sufficient to advance current graph truth. It must not infer graph
advancement from request construction or from `EffectUnsupported`.

## Gate B Milestone Review

Gate B is complete on the integrated Roadmap 0008 branch. It establishes a
pure execution language without performing or persisting an external effect:

```text
ActivityPlan
  x SagaState
    -> ExecutionSchedule
      -> EffectRequest
        -> EffectInterpreter protocol
          -> typed EffectResult
```

The concrete objects are:

```text
SagaProgram e       immutable callback-free composition syntax
SagaState           event-reconstructed step and compensation evidence
ExecutionSchedule   deterministic current classification of plan activities
EffectRequest       identity + typed action + timeout + secret references
EffectResult        succeeded | failed | unsupported
```

The major transformations remain separate:

```text
decide(SagaProgram, SagaState, SagaCommand) -> SagaEvent*
evolve(SagaProgram, SagaState, SagaEvent) -> SagaState
derive_schedule(ActivityPlan, SagaState) -> ExecutionSchedule
effect_request_for_activity(PlannedActivity, identity) -> EffectRequest
dispatch_effect(EffectRequest, EffectInterpreter) -> EffectResult
```

No transformation persists a cursor, mutates graph truth, opens a transaction,
or contacts a provider. `ActivityPlan` remains the canonical dependency graph;
`SagaState` remains reconstructed evidence; `ExecutionSchedule` is a derived
projection; and the effect language is a provider-neutral boundary.

### Executable Laws

The test suite now enforces these Gate B laws:

- saga syntax is immutable data and contains no callback effects;
- replaying the same ordered events reconstructs the same `SagaState`;
- parallel branches expose fan-out without inventing a compensation for the
  parallel node itself;
- compensation candidates are completed leaves in reverse durable completion
  order;
- failed or incomplete leaves never receive fabricated compensation;
- the same plan and evidence derive the same schedule;
- failed dependencies block their descendants transitively;
- cancellation schedules no new forward work but preserves in-flight evidence;
- compensation waits until forward and compensation work in flight settles;
- review-blocked planning work never becomes an effect request;
- every safe `ActivityOperation` maps to one typed capability and can execute
  through the fake interpreter;
- absent capability returns `EffectUnsupported` without calling an interpreter;
- attempted failure is distinct from unsupported work;
- interpreter results must preserve exact request identity;
- timeout policy is bounded and endpoint/secret inputs are opaque references;
- pure saga, scheduling, and effect packages import no store, workflow, Docker,
  server, HTTP client, or process-execution implementation.

### Integrated Validation

The merged roadmap branch passed the complete Docker/Postgres suite:

```text
Ran 407 tests in 32.462s
OK
```

The Gate B diff from the hardened Gate A baseline adds 2,730 lines across 17
files: 1,562 implementation/public-API lines, 998 focused test lines, and 170
learning-record lines. Test-integrity inspection
found no added skip, xfail, placeholder assertion, or provider mock replacing
application behavior. The only changed assertion in an existing test replaces
a source-substring dependency check with AST inspection of actual imports; this
strengthens the intended architectural boundary and removes docstring false
positives.

### Architecture Review

No duplicate durable model was introduced. Gate B deliberately has:

```text
one plan topology       ActivityPlan
one evidence projection SagaState
one derived schedule    ExecutionSchedule
one effect boundary     EffectRequest / EffectResult / EffectInterpreter
```

The effect algebra reuses `ActivityOperation` rather than creating provider-
specific lifecycle variants. `EffectCapability` describes interpreter powers;
the existing block `CapabilityName` describes advertised block-control powers.
Gate D adapters must map between those concepts explicitly where appropriate,
not collapse them through string coincidence.

No CPI, Auth, Router, Docker, Postgres, or application-domain name specializes
the saga, scheduler, or effect algebra. No Gate C coordinator or persistence
model was pulled forward.

### Security Review

Gate B creates no network listener or client and performs no authorization.
Secret and endpoint inputs are reference identities, never resolved values.
Evidence remains bounded by the existing execution grammar. Unsupported work
is rejected before an interpreter call.

The following obligations remain downstream:

- secret providers must resolve references without copying values into events,
  descriptors, logs, or errors;
- HTTP adapters must enforce control-route authentication, redirect policy,
  SSRF defenses, payload bounds, and timeouts;
- Docker adapters must verify ownership before mutation;
- interpreter capability advertisements must come from trusted construction,
  not untrusted provider claims;
- idempotency identity is represented here, but concrete providers must honor
  or explicitly report their idempotency guarantees.

### Data-Engineering Review

Gate B contains no store, SQL, transaction, or mutable durable cursor. The
Postgres UnitOfWork law is therefore preserved vacuously: there is no database
operation to commit. The full suite still exercises the Gate A Postgres schema,
concurrency, rollback, and one-winner claim laws.

Gate C must introduce the first persistence adaptation under the external-
effect law:

```text
short transaction: record intent/event
  -> commit
    -> bounded fake effect
      -> short transaction: record result/event/observation
```

No transaction or lock may span the interpreter call. An effect attempt without
a durable result must become explicit uncertainty rather than inferred success
or automatic retry.

### Provisional Behavior And Residual Risk

- Effect dispatch is synchronous and in-memory at this gate; bounded provider
  execution and durable coordination belong to later gates.
- `EffectResult` is typed evidence but is not yet adapted into the canonical
  `ActivityEvent` journal or `ObservedState` store.
- Saga state reconstructs from pure saga events; #219 must adapt canonical
  durable activity events without creating a parallel journal.
- Scheduler output is a projection, not a lease or worker claim. #219 must use
  Gate A ownership evidence before dispatch.
- Idempotency keys prevent no side effect by themselves. The coordinator and
  provider adapter must preserve them across retries and uncertain outcomes.
- Target registration, activation, drain, and observation are language values;
  no transport or control-route semantics are claimed yet.
- Database endpoint cutover remains a reference switch only. No migration,
  replication, consistency, or retirement behavior is implied.

### Roadmap Deviations

Gate B followed the amended topology. Two focused hardening issues were added:
#250 clarified parallel compensation ownership, and #252 hardened scheduler
evidence coherence and in-flight compensation barriers. Issue #218 resolved two
local review findings inline: complete safe-operation dispatch coverage and
complete public protocol/result exports. Its dependency guard was upgraded from
source substring matching to AST import inspection after a docstring produced a
false positive.

These changes refine the intended laws; they do not add provider behavior,
persistence, or a competing model.

### Readiness Decision

Gate B is safe to accept. Gate C may begin with #244 and then #219, but only
after explicit operator instruction. Gate C must preserve these handoffs:

1. graph truth advances only from sufficient durable success evidence;
2. `EffectUnsupported` and request construction are never success evidence;
3. canonical `ActivityEvent` records become the saga journal adapter;
4. no transaction spans an effect;
5. crash windows produce explicit uncertainty;
6. the coordinator consumes the existing `ActivityPlan`, `SagaState`,
   `ExecutionSchedule`, and effect language instead of replacing them.

Implementation stops at Gate B.

## Gate B.1 AST Hardening Blocker

Before Gate C, issue parent #257 makes selected repository laws executable
through static Python analysis. The topology is:

```text
#260 reusable AST source and policy model
  -> #261 package dependency and transport isolation
  -> #258 generated Python server template validation
  -> #264 UnitOfWork commit and environment access ownership
  -> #262 test-integrity declaration audit
  -> #259 read-only FastAPI route audit

#261 + #258 + #264 + #262 + #259
  -> #263 integrated review
    -> #244 Gate C
```

The governing boundary is:

```text
AST guards static structure
behavioral tests guard semantics
runtime route inspection guards realized APIs
```

Policies must encode reviewed exceptions rather than blanket token bans.
Workflow application services legitimately own UnitOfWork commit requests;
runtime adapters may own process execution; generated servers and bootstrap
code may read process environment; and optional-dependency tests may use
conditional skips. Static analysis must make those boundaries visible without
claiming to know runtime receiver types, authorization correctness, transaction
atomicity, or external behavior.

### Gate B.1 Integrated Review Result

Gate B.1 is complete. Its implementation established one shared static-analysis
language and several small policy interpreters rather than independent source
scanners:

```text
Python source
  -> immutable SourceFacts
    -> package/transport policy
    -> commit/environment policy
    -> test-integrity policy
    -> read-route policy
      -> deterministic findings and review evidence
```

The implementation produced these concrete laws:

1. every internal package root and allowed cross-package import edge is explicit;
2. process and HTTP-client imports belong only to declared adapters;
3. lifecycle vocabulary is shared typed data rather than persistence-owned data;
4. rendered Jinja server source must parse before becoming a Docker command;
5. workflow services may request UnitOfWork commit, while physical commit belongs
   only to `stores.unit_of_work`;
6. direct process-environment access belongs to CLI and contract/bootstrap
   boundaries;
7. unapproved skips, runtime skips, empty tests, placeholder assertions, and
   swallowed exceptions fail the integrity audit;
8. approved skips and test doubles remain visible review evidence rather than
   blanket prohibitions; and
9. read-only FastAPI modules must agree at source-declaration and realized-route
   levels.

The review found and corrected two real boundary defects. Whole-tree analysis
initially exposed an optional-name ordering bug in the AST fact model. The
dependency policy then exposed `policies.lifecycle -> stores`, which existed
only because `WorkspaceLifecycle` lived in a persistence record module. The
closed lifecycle type moved to `control_plane_kit.types`; the store package
continues to re-export it without owning it.

Test-integrity review found exactly three approved optional-FastAPI
`skipUnless` declarations and no mock/patch call evidence. Literal-false
approved guards fail closed. Static route declarations and FastAPI's realized
`/workspaces/*` route table both contain only GET routes. Existing behavioral
tests continue to prove authorization, transaction atomicity, server behavior,
and UnitOfWork semantics.

The canonical Python 3.14 Docker/Postgres harness passes 437 tests after all
Gate B.1 changes.

### Gate B.1 Limitations

The policies deliberately do not overclaim:

- AST analysis covers Python source, not Jinja template bodies or other
  languages; generated Python has its own post-render parser boundary.
- Static call ownership sees the written target name but cannot infer an
  arbitrary receiver's runtime type.
- Dynamic imports, reflection, monkey-patched routes, and values assembled only
  at runtime require behavioral or runtime inspection.
- Parsing generated source proves syntax, not correct forwarding behavior.
- Static route methods do not prove authentication correctness.
- Explicit allowlists are architecture decisions and must change through review,
  not incidental test edits.

These limitations are covered by complementary Docker/Postgres integration,
live server, authorization, UnitOfWork, and FastAPI route-table tests.

### Gate B.1 Readiness Decision

Issue #263 completes the Gate B.1 blocker. Gate C issue #244 is now
architecturally unblocked, but Roadmap 0008 still requires explicit operator
instruction before Gate C implementation begins. No external effects were
introduced by this vertical.
