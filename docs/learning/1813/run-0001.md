# Runtime control — run 0001

## Core #1825 declaration and static disclosure

Source base: e3e29995a4ffc6e6645c2b35d41f394438464d2d on roadmap/1754.
Child codex/1825-health-declarations targets roadmap/1813-runtime-control;
root source integration targets roadmap/1754 before public acceptance #163.
North released this child; Kepler passed its concrete interface design.

Chosen shape: extend the existing control surface with a closed health_reads
tuple. Keep variable-only v1 bytes and variable operation laws. Health-only
and mixed declarations use v2. Static requests/grants retain their existing
profiles; v2 results disclose the new declaration and explicitly report only
variable registry coverage. Zero variables means NONE, not health readiness.

The explicit health declaration maps to a fixed authenticated SDK health route
on its named HTTP provider. Legacy HEALTH_CHECKABLE and /__deploy descriptors
retain their bytes; their name cannot imply the new route. Product health
functions will be shared by later SDK adapters. Core installs no handler.

Governing current tests are test_node_control_surfaces.py,
test_node_control_operation_contracts.py, test_node_control_surface_read_authority.py,
test_node_control_surface_read_results.py and test_control_routes.py. Their
legacy laws remain executable. New tests in test_node_health_declarations.py
cover the canonical union, disjoint declaration profiles, control/data socket
distinction without a consumer edge, capability consistency, closed route
mapping, static disclosure correlation and exact/context byte bounds.

The tests-only checkpoint 6d23226343ee65b9c11303479fa930b7612a3891 ran through
the owning ./control-plane-kit-core/test.sh: 21 support tests passed; package
integrity counted 742 tests with no mocks or approved skips; the package run
had exactly ten missing-NodeHealthReadKind assertion failures and no errors.
The 732 existing tests passed. This is interface red, not proof that the new
negative cases beyond that missing API had already executed. Full log SHA-256:
2811af0bbc5548c2287f7fddd13c4f93177c1b57f367e287ddcf799b6e6570a8.
No immutable parity migration or live success is claimed.

The first source candidate 50140f5af754678d02b33ae3f62ba1a3fef38927 implements
only the surface extension, profile matrix, static disclosure and closed route
mapping. Its owning suite ran 742 tests: all ten new laws passed, with one
existing exact route-set inventory assertion missing the newly declared
NODE_HEALTH entry. Add that entry while retaining every prior entry and exact
equality; existing legacy route tests and the new exact health route test remain.
No runtime behavior or assertions are weakened to resolve this witness update.
The corrected candidate 48a5cabc1d683db892f2d282f6091afcfd04d012 passed the
complete Core owning gate: 742 tests, 21 support tests, compilation and clean
import. Log SHA-256:
f3fab9c042f11b611531b12d3068210e2bc364eeef123e2a4b7c716a4e819dac.

The same candidate passed the complete Operations owning gate: 1,546 tests,
compilation and clean import. It used a clean architecture-testing checkout at
7ebc362da40e9d7b2bdf78357e6ed8abd9a275ef and isolated test resource names;
both its PostgreSQL container and network were verified absent after owning
cleanup. Log SHA-256:
e97d1a15240753feb8a0beb393b90ac8acdece111215f1c4cb91f9df2c9c62b4.

The ordinary current-backend gate also passed all nine stages, including the
HTTP/MCP source-live scenario and residue audit. This is **locked baseline**
evidence, not candidate cross-repository adoption. Runner commit was 48a5cabc;
its resolved immutable source coordinates were:

| Repository | Commit |
| --- | --- |
| Core/Operations | f45384e72a79f59c93a715fd08f409f86a91218a |
| Interpreters | 2335a21adc5c0b0ae2f592bd15757c6ca1a55e4b |
| Secrets | 96e86dc3248d578780d64d5d7fc5d6359631d1d6 |
| Servers | 43e9f359ca828c83fe4994ed1b62e1be54277ddd |

Bounded report SHA-256:
bdd88fa1eeffbba2f7f6efed509ac1ab7d1d2f5c3aa130d95b32e842516e52a5.
The supported runner has no candidate override. Later reviewed Servers
coordinates/pins and root lock adoption must select the actual candidate before
the same gate can prove that composition; Servers #181/#191/#192 retain this
handoff. No alternate runner or cross-repository pin change is included here.

Hosted [package checks](https://github.com/OpenJ92/control-plane-kit/actions/runs/34710958551)
also passed at exact candidate 48a5cabc. Kepler found no actionable source or
architecture issue. [PR #1828](https://github.com/OpenJ92/control-plane-kit/pull/1828)
remains draft pending Meridian's independent review; task messaging failure
does not waive that review. This final evidence update changes documentation
only; tested application source and tests remain those of 48a5cabc.

No modules move or are added: the current package module
inventory remains unchanged, and the JSON module inventory explicitly marked
historical pre-extraction evidence is not rewritten as current ownership.
New companions describe each changed source owner.

Security/data/history: pure declarations and protocol data only. No signer,
credential, transaction, store, callback, network or provider behavior changes.
Workload observations/authorization and gateway transit remain #1826/#1827;
their exact profile and observation freshness gates remain. Python wrappers
precede native packaging; neither is implemented here. Meridian's independent
PR review is required before merge.

## Core #1826: semantic workload health read

Predecessor #1825 merged through PR #1828 at
397ff12f9b89bbdd32d1ef7d864bc02256bd2b21 after independent review. Its earlier
pending-review paragraph above records that earlier checkpoint, not current
merge state. #1826 starts from this exact integration commit on
codex/1826-workload-health-read, targeting roadmap/1813-runtime-control.

Kepler passed the pure design: dedicated WORKLOAD_NODE_HEALTH_READ purpose,
RUNTIME nominal reference, independent expected local target/runtime/declaration
and actual route kind, exact request-bound four-outcome result, and fresh
request ID/digest per new logical observation. No opaque authorization-context
digest, Core attempt state, runtime effect or additional SDK installer is added.
The current backend schema and secret signing intents require explicit later
adoption in #1821/#149 with appropriate Core/Secrets owners; no purpose aliases.

Tests-only checkpoint a397b8c ran the ordinary owning Core test.sh. Support21
passed and all 750 package tests collected: existing742 passed, eight new tests
failed solely on the missing NodeHealthReadRequest public API, with no errors.
This is interface-red evidence only, not a claim that assertions beyond the
missing API executed. Red log SHA-256:
56f8ddb35f8d5f56b5619c527379a8ba8566f385ac8071d74f20a4f4e62c65bb.
Source review then corrected the old command-grant fixture to retain its
APPLY_COMMAND codec; family-denial assertions remain intact.

The first implementation checkpoint is unvalidated. New pure owners are
node_health_reads.py and node_health_read_results.py, with explicit current
Core module inventory and companion updates. Historical pre-extraction JSON
inventory remains historical. Request/grant/result bounds 1083/2107/446 have
design arithmetic only until the owning suite proves their reachable witnesses.

Security/data/history: unsigned pure claims only; no exposure, key provisioning,
secret use, callback, transaction or provider mutation. Exact local binding
must precede SDK callbacks. Operations owns current admission, fresh request
allocation, retry/late-response history and deadlines. Short issued validity
is not immediate revocation; request digests alone are not authenticated or
fresh provider evidence. Static status and old profiles retain their meaning.
Candidate Core/Operations and ordinary locked-baseline current-backend evidence
remain outstanding at this checkpoint; later Servers coordinate adoption must
prove actual candidate composition. Meridian reviews the PR before merge.
