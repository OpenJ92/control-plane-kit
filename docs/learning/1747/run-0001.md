# Roadmap 1747 product loop: saved topology intent

Status (2026-09-07): #1762, #1763 and #1764 accepted; parent #1749 closed.
#1752 client composition is in progress; integrated acceptance remains pending.

Parent #1749 was split after Meridian's review found that catalogue persistence,
selection/deletion and preparation required distinct transaction and replay
reviews. The abandoned monolithic ceiling has no implementation authority.
Children target `roadmap/1747-product-loop`; roadmap PR #1765 remains the draft
integration container into develop.

## Accepted dependencies

#1762 / PR #1766 merged at `0d590b32141e698764434807a660260ec032f7df`.
It introduced workspace-scoped draft identity and append-only revisions, existing
immutable graph storage, authenticated create/revise/read, composite tenant
integrity and a deferred head cycle. Core698 and Operations1434 owning suites
were green, with hosted checks and independent review accepted. Draft writes do
not publish desired truth or perform effects.

#1763 / PR #1767 merged at `783c1e4658496acd6fc2b95d41402a022f6d9d52`.
It selects exact immutable graph identity through the existing desired
projection/generation tuple, increments generation even for same-graph selection,
and tombstones only unused drafts. Separate indexed plan-base and plan-desired
EXISTS checks include all retained statuses and every revision. Planning shares
the workspace lock; no planner mutation was needed. Current/desired references
and retained plans protect history from tombstoning. Core699 and Operations1454
fully passed, including compile/import and cleanup, followed by hosted checks
and Meridian final review. Kepler's independent review also passed.

The schema at this boundary is39 relations/503 columns/379 constraints/127
indexes/85 FKs, semantic SHA256
`6e4ef9ff20f9faceb9e135f4a2591f49ed5a247a84802947fb4cee34f584076a`.
The existing reset-on-drift policy is unchanged; no retained namespace reset or
migration was performed. Product ACTIVE reread is not a revocation fence.

## #1764 chosen shape and review learning

The law-context preceded source dry run and tests. Inline preparation's existing
session -> desired publication -> plan -> approval-request composition cannot
consume a saved graph unchanged: it would copy/re-publish identity and increment
generation. Starting the session first would also record evidence before saved
admission. A bounded saved-admission service therefore validates exact selection
and full lineage fences before the existing session/start action commit. The
session writer is extracted into a transaction-taking operation; it retains the
same owner, fingerprint/action semantics and never commits its caller's UoW.
The existing DeploymentProgram then uses unchanged planning/approval services.
No generic workflow hook, new planner or all-stage transaction was added.

Lock order is session idempotency key -> workspace -> draft before a new session
exists. Later stages retain their own session/workspace locks and commit
boundaries. Admission is not a reservation: later selection drift blocks first
planning, leaving truthful partial session evidence. Completed replay resolves
historical coordinates before mutable admission. Exact inline v1 digests, child
keys and restart behavior remain governing tests.

Meridian initially held the test packet because metadata/digest closure was only
partly executable. The corrected packet fixes all nine string fields, an exact
saved marker and seven reserved keys; independently computes canonical intent;
tests every orphan/missing key and each lineage/generation corruption; and
requires the admission owner to compute the digest. Overview uses only its
already-selected plan/session, validates the committed mapping, and returns a
bounded prepared coordinate. Complete erasure of saved metadata cannot be
distinguished from legacy and is documented rather than inferred.

Immutable red checkpoint: `dbdca92daa12d0c45d9da05e28ac1bb744c7a4f7`, four
reviewed tests/support files,20 methods. Core699 fully green with compile/import;
Operations1474 produced exactly20 missing-saved-input failures, zero errors or
unrelated failures; cleanup confirmed. Meridian accepted causal red.
Core log SHA256 `ccc84495e95567641330ebce2fe70c2dd8f51cba27311f021e72634bb46aff36`.
Operations red log SHA256 `f41802b243b85d7117f2549a654055b9cd51721d27cb82757ead4430277087de`.

## #1764 source review and owning green

Meridian source-static PASS at `8d6473ec99ee477a2fe3d22a9c42ce4ab238b0f4`
(tree `e65393213810ef92dbb5d9afb84975199378b89a`) found no blockers.
Core remains byte-identical to its retained full-green 699-test gate, including
compile/import; its log SHA256 is
`ccc84495e95567641330ebce2fe70c2dd8f51cba27311f021e72634bb46aff36`.
The unmodified owning Operations Docker suite at that exact source checkpoint
passed all 1,474 tests in 1588.037 seconds, with three declared mocks, zero
approved skips, compilation and clean import complete, and exit 0. Operations
green log SHA256 is
`2800931e921f7df3db1f76530335aa0dbce6752eb1e276111e56467fa849fb8d`.
The exact disposable container and network were absent after cleanup. Source
and all four reviewed red-test hashes remained unchanged during execution.

## Historical pre-merge checkpoint and residual

At the pre-merge checkpoint, #1764 final PR review, hosted checks, merge and
acceptance were pending. Parent completion was not yet claimed and this
checkpoint released no #1752 work. The dated acceptance below supersedes that
status while retaining its historical evidence.
Core, schema, graph algebra, planner/approval semantics, selection, providers and
runtime remain outside this change. Preparation grants neither approval nor
execution authority and writes no graph/projection/current/desired truth.
Security relies on existing workspace-edit plus plan-request authorization;
history is bounded and omits graph bodies and credentials. Staged partial
history, product revocation races and complete metadata erasure retain the
explicit limits above. #1752 receives a handoff only after #1764 acceptance.

## Acceptance and next work — 2026-09-07

#1764 / [PR #1768](https://github.com/OpenJ92/control-plane-kit/pull/1768)
merged into this roadmap at
`e3d773d022fd36e727ee1d94f4c4396b25c722c6`. Meridian's final review and hosted
Core, Operations and current-backend checks passed. The accepted owning evidence
is Core699 and Operations1474 as recorded above; no fresh suite was run merely
to correct this document. Parent #1749 is closed.

Kepler's full review passed, with one nonblocking documentation finding: this
learning record still described acceptance as pending. This dated update closes
that finding without changing the accepted application or test source.

The [#1752 handoff](https://github.com/OpenJ92/control-plane-kit/issues/1752#issuecomment-5565041163)
records the saved-revision contract. Jacob subsequently authorized shared team
design, then uninterrupted work through three ordered server-client PRs:
[servers #137](https://github.com/OpenJ92/control-plane-kit-servers/issues/137),
[#138](https://github.com/OpenJ92/control-plane-kit-servers/issues/138), then
[#139](https://github.com/OpenJ92/control-plane-kit-servers/issues/139).
Vale implements, Meridian reviews each PR, North coordinates, and Kepler reviews
the integrated result after all three. Server roadmap PR #140 and CPK roadmap
PR #1765 remain draft integration containers. Later held work remains held.

No live product-loop acceptance, provider mutation or teardown is claimed or
authorized by this record. Security, data and operational limits above remain.
