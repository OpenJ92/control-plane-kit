# Application readiness run 0001 — 2026-09-07

## Boundary and inherited evidence

The application-readiness roadmap starts at `79193957e4dfa403ed5cc60003be745d7e6c1262`,
accepted #1769/PR1770 tests-only merge. Core/Operations application source remains
inherited from the #1747 roadmap. Parent #1747/#1752 acceptance and PR1765 still
gate integration/promotion; starting isolated #1772 implementation does not
approve a live plan or upgrade the retained acceptance installation.

## #1772 decisions

Jacob explicitly released this slice after the North/Meridian/Kepler design
review. One relational source links each saved preparation session to its exact
same-workspace revision. Existing metadata/fingerprint remains the immutable
admission commitment; disagreement is evidence failure, not repair authority.
Graph identity derives through revision, with no redundant graph column, new
metadata version, deployment slots, or per-plan/run source copies.

Fresh admission retains session-key -> workspace -> draft locks. Session/start
and source commit atomically; later planning and approval keep their existing
staged transactions. Replay requires the original source and immutable evidence
before current-state admission. Saving a revision alone creates no source.

Meridian required bidirectional retained-data checks beyond FKs. The existing
schema verifier traverses saved-key OR source-owner session candidates with
SQL byte guards and batches of at most64 under its existing transaction/locks.
Kepler caught the partial-evidence case: orphan saved keys must be candidates
even without the exact discriminator. Memory/transport are bounded; total scan
work and lock duration are not. There is no per-HTTP global scan, audit engine
or repair. Completely erased saved metadata plus missing source remains
indistinguishable; tests preserve this limit.

## Tests and source evidence

Meridian tests-only static PASS covers 13 real-Postgres owner tests and one
existing HTTP/MCP overview addition. Immutable tests commit
`fa9e0d0964bb93eef65b5c670a6a8614ac1ccfc5`: two files,254 added lines; original
fixtures and assertions unchanged. Unmodified owning Operations causal red ran
1,489 tests in1444.798s: exactly14 explicit missing-source-relation failures,
zero errors or unrelated failures; existing1,475 tests passed. Package integrity
reported three existing mocks and zero approved skips. Compilation/import were
not reached after the intended behavioral failure. Disposable container/network
cleanup was verified.

Red log SHA256 `d2f0e282fc025ef3d45ca8431a1a0b85f3284e472964448127fac8e3683324be`.
The outer zsh capture then failed because `status` is read-only, so a separate
suite exit file was not produced; the tool exit was1 and the complete unittest
failure summary/cleanup remain authoritative. No suite rerun or fallback was
used to disguise this capture limitation.

Source implementation and additive schema/inventory witnesses passed owning
validation and independent source review at `27d14b3`, as recorded below.
PR merge disposition remains with North.
The schema is40 relations/507 columns/382 constraints/129 indexes/87 foreign keys.
No Core language, planner/approval/execution, provider or live data behavior was
changed. The new schema is fresh-exact only; older installations reject before
DDL and remain pinned/readable. No reset, migration, backfill or history import
is authorized or implemented.

## Handoff and security

#1773 history remains a separate reviewed specification; #1774 restoration is
also separate. Source proves explicit input, not successful execution, accepted
current advancement or provider health. Existing workspace edit plus plan
request authorization remains; returned evidence is bounded and omits metadata,
graph bodies and credentials. Any retained-data transition needs its own
inspectable data-preserving plan and authorization.

### First owning source run and bounded correction

At source `187527ea1727c5e685ab01ab884e1dc1b434463e`, all fourteen target laws
passed. The unmodified Operations suite ran 1,489 tests in 1462.781 seconds:
eight additive witness failures, zero errors. Five failures shared an omitted
relation-name tuple; the others were the CREATE-table count, atlas section
ordering, and foreign-key count. Those exact expectations were corrected.
Log SHA256 `b50f0933eb86ffa6902a28097de3fa3206eeb78068e54839236bca00444e0608`;
captured suite exit 1, disposable container/network absent afterward.
Compile/import remained uncredited after fail-fast. Hosted Core and
current-backend passed at that source; final Operations evidence remains pending.

Static self-review also found that an initial empty-string keyset cursor could
omit an empty-ID malformed candidate permitted by the old session SQL. A focused
real-store negative test was added before changing the scan to a nullable first
cursor, so all candidates enter bounded decoding. This is a completeness fix,
not a new schema rule or repair. The original red checkpoint remains immutable;
the owner test had this one added law, while the adapter test was unchanged at
that checkpoint.
No separately executed red is claimed for the static-review finding.

### Final correction, validation, and review

The second owning run at `72f9b5b` ran 1,490 tests in 1470.554 seconds with
exactly one failure: the raw SQL checksum witness still contained its previous
value. All 15 added provenance laws passed, with zero errors. Exit 1 and ordinary
disposable cleanup were confirmed; compilation/import were not reached. Log
SHA256 `a715e224b5d45dffbff7a212db4eac82371cc3ab9416ac6df2fbc7a0e3082f61`.

Meridian also found two static defects: the new inventory entry had been placed
inside the ownership vocabulary, and overview returned `none` for stripped saved
metadata despite a surviving source. The bounded correction moved the exact
entry into modules, restored the six ownership strings, recorded the new known
consumer, and consulted the tenant-scoped source before returning `none`.
The existing adapter test now proves retained source plus stripped commitment
is unavailable without writes; explicit erasure of both still yields `none`
without writes. These corrections claim no separately executed target-red.

Validated source head: `27d14b366e81cc7b87ee06e86c7c9d89ae2baf94`.
The unmodified `./control-plane-kit-operations/test.sh` passed all 1,490 tests
in 1469.873 seconds, with three declared mocks and zero skips. Compilation and
the clean package import also passed; suite exit was 0. The exact issue-owned
`cpk-1772-green3` network and its containers were absent after ordinary cleanup.
Log SHA256 `4be18284b089414d42fe1db005f2bae06c16d80ea360c39b06d8ac950184823c`.
The architecture-testing prerequisite was clean at
`7ebc362da40e9d7b2bdf78357e6ed8abd9a275ef`. No host test runner or custom harness
was used.

[Hosted Core and Operations](https://github.com/OpenJ92/control-plane-kit/actions/runs/34164758118)
and [current backend composition](https://github.com/OpenJ92/control-plane-kit/actions/runs/34164758080)
also passed at that exact source head.
[Meridian's final source review](https://github.com/OpenJ92/control-plane-kit/pull/1776#issuecomment-5576108730)
and [Kepler's integrated review](https://github.com/OpenJ92/control-plane-kit/pull/1776#issuecomment-5576082954)
both report PASS with no remaining source finding. This closeout changes only
learning documentation; the validated production, test, schema, and harness
bytes remain those of the named source head.

North retains merge disposition. #1773/#1774 source work and roadmap promotion
remain separately gated. No retained schema, credentials, provider resources,
or the unapproved #1752 plan were changed. Input provenance remains distinct
from approval, execution, accepted advancement, and provider truth; total
install-time validation work and lock duration remain unbounded by batch size.
