# HARDEN.PROGRAM Run 0001: Canonical Run Identity Convergence

Date: 2026-08-14
Milestone issue: #1634
Accepted source coordinate: `develop@03a85a1834ed61474ac71483555c802170540619`
Next issue: #1626

## Purpose

This milestone reviewed the complete canonical run-identity vertical before a
durable deployment-program interpreter reconstructs projections from current
Operations records. It joined five accepted implementation children:

```text
#1636 Core nominal RunId algebra
  -> #1637 authoritative Operations run truth
    -> #1638 program/read/command propagation
      -> #1639 provenance/authority propagation
        -> #1640 exact current PostgreSQL schema
```

The milestone added no production module, test framework, schema object,
migration, compatibility path, or runtime effect. Existing child-owned tests
already provide the executable laws. This record freezes their composition,
review findings, validation evidence, and the release decision for #1626.

## Accepted Coordinates

| Issue | Reviewed head | Accepted `develop` |
| --- | --- | --- |
| #1636 | `0e0b2a364d07372d48d6ac5544d56a06edd25f7e` | `20ebe8c7584ab72352e75ee0890983d84fd281ac` |
| #1637 | `bbe138c6d9e0e9a9e924c5c807828e758f20b174` | `07c69f5622bb86fbc771bc9493c5f9723d71d258` |
| #1638 | `0fef133efcf5e5b14d3ee86b5e40c090d1d62835` | `63f2499be47e30f089b3d612db301a3ebbfff429` |
| #1639 | `2af3fb06ca32ecab92313ec33384ddcff83af5b1` | `8ec6c98277360550b507e748d395c26b866f88b6` |
| #1640 | `567365b5a41f7a850a82dd5fb9610840c7c658ba` | `03a85a1834ed61474ac71483555c802170540619` |

Every child was accepted in topological order and is closed. The milestone
started from the final accepted coordinate in a fresh worktree.

## Capability

One run identity now crosses pure algebra, Operations commands and records,
durable PostgreSQL representation, secret authority, ingress provenance,
runtime-effect translation, recovery, read scopes, and all nine run-bearing
deployment-program projections without truncation, remapping, or aliasing.

The canonical language is:

```text
[A-Za-z0-9][A-Za-z0-9._:-]{0,199}
```

One- and 200-character values are valid. Invalid types, hostile subclasses,
empty text, leading punctuation, whitespace, controls, slash, unsafe alphabet,
and 201-character values fail through the owning categorical boundary without
candidate echo or retained exception context.

## Mathematical Shape

Objects:

- `RunId`, a frozen, slotted, ordered nominal Core value;
- canonical strings at persistence, wire, selector, provenance, and
  owner-specific error boundaries;
- direct durable run carriers;
- FK-derived run references.

Transformations:

```text
canonical string
  <-> RunId
    -> effect/recovery/program values

canonical string
  -> Operations owner validation
    -> durable row or selector
      -> owner reconstruction

authoritative activity run
  -> event/prior-run foreign-key identity
```

Laws:

- `RunId(s).value == s` for canonical `s`;
- every accepted run is admissible to every direct downstream consumer;
- descriptors encode strings and decoders reconstruct nominal identity;
- current, retry, prior, event, effect-attempt, secret, and provenance identity
  preserve exact equality;
- invalid factory output causes no claim, run, event, action, or external
  effect;
- public selectors reject malformed values before UoW, store, or SQL access;
- no graph, activity-count, retry-count, or topology-scale cap exists.

## Consumer Ledger

### Core Nominal Owners

- `control_plane_kit_core.operations.RunId`;
- `EffectAttemptIdentity.run_id`;
- `RuntimeEffectSource.run_id`;
- all descriptor decoders for those values.

### Core String Representation Boundaries

- `ActivityJournalEvent.run_id`;
- optional run identity on resolution, custody, and revocation secret grants.

These retain their existing owner errors while delegating to the one private
pure grammar owner.

### Authoritative Operations Truth

- `RetryIdentity.prior_run_id`;
- `ActivityRunRecord.run_id` and `ActivityEventRecord.run_id`;
- all six existing-run lifecycle commands;
- injected run identity creation and lifecycle replay payloads;
- execution-store selectors, row decoders, event pages, and restart truth;
- activity-journal translation.

### Program, Read, And Command Boundaries

- `ExecuteActivityRun`;
- `AdvanceCurrentGraph`, its result, and replay evidence;
- `RunReadScope` and run-event cursor decoding;
- authenticated true-run HTTP/MCP routes;
- all nine run-bearing deployment-program projections;
- exact projection/effect-attempt run congruence.

The accepted `command.run.claim` route is an explicit exclusion. Its path value
identifies an execution request before a run exists and is passed unchanged as
`ClaimAndOpenActivityRun.request_id`. Executable negative coverage preserves
that behavior. #1641 owns the atomic route-language repair without an alias;
#1626 must not rename or expose this route.

### Provenance And Authority Boundaries

- ingress source and removal run evidence;
- generated ingress-secret source evidence;
- gateway rotation deployment checkpoints;
- optional secret-use command, result, grant, and correlation identity;
- runtime-effect translation from the authoritative run;
- the explicit node-control no-run witness.

### Incidental Vocabulary

Core load-run route names and protocol method annotations do not independently
admit Operations activity-run truth. A token-based AST inventory would
misclassify these names and the claim-route exception, while duplicating the
behavioral matrices above. Closed-world ownership remains executable through
the package inventory and exact PostgreSQL contract where it actually exists.

## PostgreSQL Topology

The current schema contains eight run-bearing columns.

Six direct carriers enforce the exact ASCII language with locale-stable
`COLLATE "C"` checks:

1. `cpk_activity_runs.run_id`;
2. `cpk_cloudflare_ingress_resources.source_run_id`;
3. nullable `cpk_cloudflare_ingress_resources.removed_by_run_id`;
4. `cpk_gateway_key_rotation_deployments.run_id`;
5. `cpk_generated_ingress_secret_references.source_run_id`;
6. nullable `cpk_secret_use_authorizations.run_id`.

Two carriers derive identity through validated immediate foreign keys and do
not duplicate the grammar:

1. `cpk_activity_events.run_id`;
2. nullable `cpk_activity_runs.prior_run_id`.

Accepted catalog truth is 29 relations, 380 columns, 273 constraints, 94
indexes, and 54 foreign keys. Installation remains object-free-create or exact
current-shape read-only verification. A missing, wrong, or noncanonical shape
is reset-required without repair, migration, backfill, row coercion, dual read,
or row/object loss during rejection.

## Review Findings And Corrections

The five-child loop found useful boundary mistakes without changing the chosen
representation:

- #1636 classified and replaced one unusable Core gate whose terminal output
  was lost during context compaction; no false green credit was retained.
- #1637 kept authoritative persistence string-shaped and proved replay before
  identity-factory use, invalid-factory zero writes, and raw unexpected
  failures.
- #1638 translated one stale 512-character `RunReadScope` predecessor witness
  to the canonical 200-character boundary while preserving general 512-byte
  read identifiers.
- #1639 added missing real-PostgreSQL restart and malformed-row witnesses for
  ingress, generated-secret, gateway checkpoint, and secret-use evidence.
- #1640 review strengthened semantic FK-omission proof, atlas direct/derived
  section proof, and lifecycle chronology. Its first full Operations gate then
  exposed five predecessor tests that still tried to persist newly forbidden
  values. Those tests were translated to exact named CHECK/FK boundaries;
  independent review confirmed that constructor-level hostile-value matrices
  and valid decoder round trips still preserve fail-closed reconstruction.
- A transcribed #1640 review SHA was rejected before review and corrected
  durably; no ambiguous coordinate received evidence credit.

North and Vale completed independent source, amendment, PR-coordinate,
architecture, data, security, and test-integrity reviews. No unresolved finding
remains.

## Validation

All local validation used Docker-first unittest at exact accepted source
coordinate `03a85a1834ed61474ac71483555c802170540619`.

| Gate | Result |
| --- | --- |
| Installed Core identity matrix | 8/8 in 5.562s |
| Joined 17-module Operations identity matrix | 259/259 in 99.079s |
| Full Core integrity | 621 tests, 0 mocks, 0 approved skips |
| Full Core behavior | 621/621 in 16.942s |
| Full Core compile/install/outside import | green |
| Full Operations integrity | 713 tests, 0 mocks, 0 approved skips |
| Full Operations behavior | 713/713 in 453.241s |
| Full Operations compile/install/outside import | green |
| Fixed Operations cleanup and lease release | green |
| `git diff --check` and source worktree | green/clean |

The joined Operations invocation covered:

```text
activity identity
authoritative run identity
run lifecycle
runtime-effect translation and dispatch
deployment-program projections
execution coordinator
current-graph advancement
read pages
cpk-server adapters
provenance and authority identity
ingress authority
gateway rotation
secret providers
run schema
operations table atlas
current-schema installation
```

Current-backend also passed every stage with runner coordinate
`03a85a1834ed61474ac71483555c802170540619`, including cpk-server HTTP/MCP
source-live smoke and residue audit. Its source lock assembled CPK
`f45384e72a79f59c93a715fd08f409f86a91218a`, so it is supporting/indirect
compatibility evidence, not direct proof of this run-identity vertical. The
direct Core and Operations gates above govern the #1626 release decision.

The documentation-only milestone PR must still receive terminal attached Core,
Operations, and current-backend checks at its unchanged head before merge.

## Security Review

No new network, authentication, secret, provider, process, graph, service,
destructive-operation, or external-effect surface was introduced.

The accepted change narrows attacker- or corruption-controlled identity before
SQL, secret authorization, effect dispatch, and recovery. Public errors remain
bounded and candidate-free. Descriptors and history contain canonical identity
only and no secret, credential, endpoint address, or provider material. No
PostgreSQL transaction spans external IO.

Residual risk: a current-schema database that does not match the exact checks
is unavailable until reset. That is the deliberate development boundary, not
an implicit repair path.

## Data Review

Durable identity has one authoritative string language and one exact current
schema. There is no migration or live data movement, and therefore no hidden
transaction, retry, rollback, or compensation behavior. Direct evidence tables
retain their existing ownership rather than acquiring speculative FKs merely
to reuse grammar. Event and retry lineage continue to derive identity from the
authoritative run by FK.

## Test-Integrity Review

- no new wrapper test calls other tests;
- no broad source-token scan substitutes for behavior;
- no skip, xfail, mock, or legacy implementation path was added;
- all apparatus failures and lost output were classified without evidence
  credit;
- predecessor tests were translated only where stronger accepted boundaries
  made their old setup impossible;
- exact cleanup and serialized harness ownership were recorded durably.

## Operational Reliability

This milestone adds no runtime behavior. Existing health, logs, retries,
cleanup, and structured history remain unchanged. The identity law improves
restart and recovery reliability by ensuring every durable run can be consumed
by effect, secret, provenance, and projection paths after process replacement.

No new example is required for the identity value alone. #1626 should explain
the law through durable preparation replay rather than add a disconnected
identity example.

## Residuals

- #1641 repairs misleading execution-request claim route vocabulary and remains
  nonblocking unless a later issue exposes claim routing.
- current-backend remains source-locked to older CPK truth and must retain its
  supporting/indirect classification until server-product coordinates advance.
- #1626 owns effect-free preparation only. It must not absorb claim-route
  repair, approval decisions, execution, recovery effects, schema changes, or
  direct store/SQL choreography.

## Release Decision

#1626 is released after this documentation-only milestone PR is independently
reviewed, its attached checks are terminal green at an unchanged head, and
#1634 is accepted on `develop`.

The architectural reason is narrow: #1626 can now reconstruct the accepted
deployment-program projection family from durable records knowing that every
run identity is admissible to in-flight, recovery, secret, provenance, read,
command, and advancement paths. It may compose existing command services for
effect-free preparation; it may not reinterpret or weaken the identity law.
