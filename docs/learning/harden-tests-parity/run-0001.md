# HARDEN.TESTS.PARITY Run 0001

## #1315 Exact semantic migration inventory

The root validation command was not an integrated backend gate. It ran current
core and operations first, then built the mutable pre-extraction
`control_plane_kit` distribution and executed its root tests. Its final count
therefore described the legacy monolith and omitted interpreters, server
products, and secrets.

The immutable reference remains exact and recoverable:

```text
tag:    pre-server-product-extraction-2026-07-20
commit: 20129959d3b0f8e8bd5dbdafdf51c0a5d592a9ec
```

The #1315 inventory is derived from committed Git archives so dirty sibling
worktrees cannot leak into migration evidence. It records:

```text
1,090 immutable reference laws (1,112 collection occurrences)
1,210 mutable legacy test methods (1,232 collection occurrences)
  120 mutable-only post-reference methods
1,163 current committed package test methods
   21 mutable legacy helpers
   26 mutable root shell entry points (legacy, reference, and parity tooling)
    7 current package test helpers
   31 current package test/smoke shell entry points
```

The current committed method count is not a parity claim. It is only the exact
input population for the package passes. The active dirty #1311 operations test
and dirty server-products work are intentionally absent because neither is part
of the archived source commits.

The 1,232 legacy execution count is now explained rather than compared: 1,210
distinct mutable methods plus 22 inherited collection repetitions. Repetition
does not create a second behavioral law.

Every immutable law now has one provisional review queue:

```text
#1320 core
#1321 operations
#1322 interpreters
#1323 server products/processes
#1324 durable secrets across package boundaries
#1325 cross-repository, live, meta, and deferred behavior
```

The provisional immutable-law queues contain 231 core, 303 operations, 117
interpreter/runtime, 268 server-product/process, 22 cross-package secret, and
149 cross-repository/live/meta/deferred laws. These counts are workload inputs,
not parity or ownership conclusions.

The scanner exposes assertion kinds and lexical negative-case hints, but it does
not make architectural decisions. A same-named current test is only a candidate.
The package passes must inspect behavior and classify each law as an isomorphic
or strengthened successor, reviewed supersession, sufficiently detailed future
issue, or reviewed archival.

All 1,090 frozen identities resolve exactly to their source method. The mutable
root adds 20 post-reference modules, almost entirely extraction/parity tooling;
those methods are inventoried separately so #1318 cannot delete the root test
tree before enduring validators have moved to a durable home.

The script inventory deliberately distinguishes root legacy entry points from
current package gates. Useful scenarios may move into #1207's current acceptance
matrix, but the old shell implementations are deletion input, not compatibility
wrappers.

## #1320 Core language reconciliation

The 231 frozen laws provisionally assigned to core were not all equally current.
The review found 227 live core successors and four application-level deployment
transition laws owned by open HARDEN.6 issue #1096. Two mutable-only Postgres
authentication tests are strengthened by the current secret-free authentication
contract.

Seven resource-lifecycle laws had been recorded as interpreter supersessions.
That classification was too broad: lifecycle values, codec preservation, and
plan shape are pure core laws even though their later realization is an
interpreter concern. `ResourceLifecycleSuccessorTests` now proves that core:

- models ownership, compute persistence, and retained data independently;
- round-trips lifecycle through the graph codec without string inference;
- removes ephemeral compute without inventing data destruction;
- stops retained compute without removing its resource;
- emits no lifecycle work for external topology;
- requires explicit critical destructive activity for data destruction; and
- blocks lifecycle-policy mutation for review.

The reconciliation overlay records exact current test identities instead of
module-level or stale successor names. It deliberately leaves the four #1096
laws without false passing evidence. This makes the evidence more truthful even
though the foundation parity report now shows four additional required entries
without completed successor evidence.

Validation: the Docker-first core package gate passed 482 tests, compileall, and
the package import check. No runtime, persistence, network, secret, or
transaction surface changed.

## #1330 Session command serialization contract

The #1321 operations reconciliation exposed two related transaction-order
defects: identical desired-graph commands could lose replay to stale-pointer
handling, and planning could publish after a concurrent close. A broader source
audit showed that the inconsistency spans every operation-action writer rather
than only those two services.

The accepted global order is now explicit:

```text
command identity advisory lock
  -> durable replay read
    -> session row FOR UPDATE and lifecycle re-read
      -> workspace row FOR UPDATE when required
        -> narrow dependent rows
          -> action ordinal while session remains locked
            -> domain writes plus action
              -> one application-owned commit
```

`start-operation-session` remains the pre-row exception and uses its existing
workspace/idempotency creation lock. Every other action writer has one closed
role map in `control-plane-kit-operations/session-command-serialization.json`.
The structural package tests reject missing writers, duplicate roles, role-order
inversions, and incomplete replay/terminal/transaction outcomes.

This contract intentionally serializes commands inside one session while
retaining concurrency across independent sessions. It does not introduce
process locks, retries, execution leases, or external-effect recovery. Those
remain separate from this Postgres transaction-order correction.

## #1331 Postgres session serialization primitives

The activity-history store now exposes exactly two implementation primitives
for the #1330 contract:

- `lock_action_idempotency(session_id, idempotency_key)` takes a namespaced
  transaction advisory lock before an action row exists;
- `get_session_for_update(session_id)` selects and returns the complete
  authoritative session lifecycle row under `FOR UPDATE`.

The advisory lock is command identity, not lifecycle authority. The session row
lock is lifecycle and action-order authority. Keeping those roles separate lets
identical commands converge before pointer validation while still serializing
distinct commands that mutate one session.

Real Postgres tests with independent connections proved that a duplicate
identity blocks, the same key in another session does not block, a later row
lock owner sees a committed terminal status, concurrent action writers allocate
unique monotonic ordinals, and rollback releases both lock types while removing
partial action evidence. `lock_timeout` supplies bounded blocking proof without
sleeps or process-local coordination.

The primitives do not commit, roll back, retry, or accept caller-selected lock
names. The application UnitOfWork remains the sole transaction owner.

## #1332 Desired graph replay convergence

`DesiredGraphCommandService` now acquires the session/action identity advisory
lock before reading replay evidence. A missing replay then leads to the exact
session row `FOR UPDATE`, workspace ownership and `OPEN` validation, and only
then the existing workspace compare-and-set and action append.

This order makes two superficially similar races intentionally different:

```text
same key + same intent
  -> wait for the first transaction
    -> replay its exact graph, projection, revision, and action

distinct keys + one expected pointer
  -> serialize on session lifecycle
    -> one command mutates the pointer
      -> the other reports truthful stale state
```

The previously failing #1321 concurrency test was copied unchanged and now
passes. Additional real Postgres evidence proves one desired graph version, one
realized projection, one workspace revision increment, and one action for the
identical race. Exact replay also survives a later desired pointer change and
session close because durable replay is resolved before current lifecycle or
pointer validation.

No retry loop was added, stale semantics were not weakened, and graph truth and
action evidence remain one UnitOfWork transaction.

## #1333 Planning and terminal-session fencing

Activity planning and close/cancel now share the same identity-first session
order. Planning resolves durable replay before state validation, locks and
re-reads the session, requires `OPEN`, and only then locks workspace graph truth.
Close and cancel resolve replay under their command identity, then lock the
session before terminal validation, ordinal allocation, transition, and action.

The resulting race has only two legal durable histories:

```text
request-activity-plan -> close-or-cancel

close-or-cancel
  -> planning observes terminal state
    -> no plan or planning action
```

The formerly observed `close-operation-session -> request-activity-plan` order
is impossible because planning cannot validate `OPEN` from an unlocked snapshot.
Exact plan and terminal replays remain available after terminal state because
committed evidence is read before current lifecycle validation.

Directed real Postgres tests use injected clocks as transaction barriers and a
connection observer to prove the actual session `FOR UPDATE` statement was
attempted or acquired. They do not use sleeps, process-local production locks,
or service test hooks. Bounded futures prove planning-before-terminal,
terminal-before-planning for both close and cancel, identical plan convergence,
write-once terminal state, and absence of deadlock in these lock orders.

## #1334 Cross-service session concurrency audit

The final audit found that the late-session-lock pattern still existed in
manual actions, approvals, execution admission, run lifecycle, desired realized
projection publication, and current graph advancement. Each path now follows
the same identity-first lifecycle fence. Run and advancement commands use only
immutable request/run linkage to locate the session before locking; mutable
truth is re-read afterward.

Gateway key-rotation projection composition exposed one less obvious inversion.
The outer rotation service derived dependent rotation and workspace material
before the generic projection writer could acquire its session lock. The outer
service now prepares command/session serialization first. Projection derivation
uses immutable rotation-to-workspace linkage as a locator, then locks workspace
before the rotation row. This preserves the global order without introducing a
rotation-specific action model.

Real Postgres acceptance now covers deterministic manual-action versus close in
both directions, approval request versus close/cancel, identical and competing
approval decisions, concurrent close versus cancel across bounded repetitions,
independent-session progress, admission/lifecycle/advancement replay after
terminal state, and rejection of fresh work after terminal state. Structural
tests discover every current operation-action writer and reject missing or
inverted identity/session/ordinal roles.

The preserved #1321 worktree must not be applied wholesale. Its desired-graph,
planning, approval-decision, and terminal-race additions are now absorbed or
superseded by #1332 through #1334. Its reconciliation builder, semantic decision
overlay, coordinator replay, schema rollback, read-service observation laws,
and still-useful start-session/current ordinal parity evidence remain inputs to
the resumed 303-law mapping.

## #1321 Operations semantic reconciliation

The operations queue contained 303 immutable laws. Reviewing them against the
merged session-serialization baseline produced four honest dispositions:

```text
239 current core/operations successors
 41 future recovery/fencing laws owned by #1092
 22 future executable DeploymentProgram laws owned by #1096
  1 reviewed supersession
```

The reconciliation builder now scans an explicit list of live distributions.
That was required because this operations-oriented queue contains both durable
operations behavior and pure execution/recovery/saga contracts that correctly
live in core. Review ownership is derived from exact selected test identities;
the old provisional package label is no longer trusted as current truth.

The one superseded law allowed an operator-confirmed uncertain result to advance
current graph truth. Current CPK deliberately rejects that override. Current
graph advancement requires complete accepted realization evidence, so the
stronger failure-closed law replaces the frozen behavior explicitly rather than
being hidden behind a misleading successor test.

The useful pre-tangent work was reconstructed selectively. New current tests
prove completed coordinator replay does not redispatch an effect, incompatible
schema installation rolls back partial DDL, observation freshness has an exact
time boundary, malformed and future timestamps fail closed, correlated probe
records require complete typed identity, identical session starts converge,
and cancellation replay cannot reopen a terminal session. Desired-graph,
planning, approval, terminal-transition, and ordinal race tests from the old
worktree were not copied because #1332 through #1334 now provide stronger real
Postgres evidence.

The 63 future laws are not placeholder omissions. #1092 receives the exact
lease-expiry, compensation, uncertain-effect, stale-worker, recovery projection,
and crash-folding obligations. #1096 receives the exact callable stage,
Postgres scenario runner, no-op, full pipeline, and replay obligations. Both
issues retain the negative cases that must not be lost when those programs are
implemented.

Validation used the live package only. The Docker-first operations gate passed
377 tests against real Postgres, followed by compileall and import validation.
The reconciliation validator proved all 303 laws have one reviewed disposition
and every current successor identity exists in live core or operations tests.
