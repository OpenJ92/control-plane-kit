Source: [control-plane-kit-core/tests/test_product_runtime_contract.py](../../../../control-plane-kit-core/tests/test_product_runtime_contract.py).
Maintain this document alongside its source file. When assertions, fixtures or relevant imported contracts change, verify and update this companion in the same change.

Twenty-three tests describe admission, normalization and selected composition laws for ProductRuntimeContract in [products.py](../../../../control-plane-kit-core/src/control_plane_kit_core/products.py). They construct pure values and graph findings. DockerRuntime, provider ports, retained mounts and verification checks are declarations here; no container, mount, secret provider or health check is executed.

## Socket admission and ordering

Three two-socket permutations independently reverse requirements, providers or both. ProductRuntimeContract must create a fresh exact BlockSockets value, sort each direction by its exact name, compare equal to the canonical contract and preserve descriptor equality. Codec round-trips cover the same permutations. A separate test keeps [BlockSockets](../../../../control-plane-kit-core/src/control_plane_kit_core/algebra.py) itself in author order: canonicalization belongs to the product contract, not every authoring value.

Seven hostile candidates cover an outer socket-container subclass, tuple subclasses in either direction, socket-member subclasses and string-subclass names. Each must yield the exact ProductRuntimeContractError and fixed message, with no recorded hostile dispatch, cause, context or candidate text. The dispatch log is cleared before each contract construction, so this measures that admission boundary rather than all fixture creation. Actual exact-type checks precede member-name lookup, hashing and sorting. The guarantee does not extend automatically to other imported values or all downstream validation.

Duplicates fail separately within each direction, while the same name may occur on both sides. Empty and mixed-case/punctuation names remain accepted and sorted; this change adds no new name grammar. A propagation example checks configured socket order, endpoint insertion order and filtered missing-provider/missing-required-connection finding order after endpoints are deliberately removed. It does not assert the complete finding sequence. Selected [graph validation loops](../../../../control-plane-kit-core/src/control_plane_kit_core/topology/validation.py) follow the socket tuples.

A separate test replaces _validate_verification with a function raising one RuntimeError, asserts that exact exception object escapes, and restores the helper in finally. Thus clean hostile-socket rejection must not be described as universal exception normalization; direct-constructor unexpected failures deliberately remain raw.

## Declared material and rejection examples

The composite fixture includes sockets, provider port, public environment, configuration, secret reference/use, retained mount, health capability/check and lifecycle. Its assertions select protocol, capability, port, mount and secret-delivery descriptor fields, not every field of that fixture. Additional codec examples cover a smaller HTTP contract, port and retained mount. Requirement-secret encoding retains a reference/use descriptor and round-trips; when no deliveries exist, the field remains omitted. Its absent-canary assertion uses a canary never inserted into that fixture, so it is not active redaction evidence. Duplicate secret environment targets fail at RequirementSocket construction before a product contract is created.

Port cases reject an unknown provider and 0, 65536 or Boolean port values. Mount cases reject five selected relative/reserved/traversal paths and a resource absent from lifecycle data. A configuration artifact sharing a lifecycle data-resource ID also fails. The actual helpers compare identities and a finite path policy; these cases do not prove every host path is excluded or exercise data creation, retention or cleanup. One HTTP check over a Postgres provider fails protocol compatibility before any effects.

The secret-literal cases fail in [PublicStaticEnvironmentBinding](../../../../control-plane-kit-core/src/control_plane_kit_core/environment.py) and [ConfigurationArtifact](../../../../control-plane-kit-core/src/control_plane_kit_core/configuration.py) argument construction: a token-shaped name and JSON password key are rejected before ProductRuntimeContract receives them. The codec separately rejects an unknown top-level field, a secret-shaped public binding and one integer provider name. Those cases do not establish arbitrary-value secret detection or uniform cause/context removal across nested decoding. The two ProductFamily assertions pin server/data-service spellings, not the enum's exhaustive membership or unknown-value rejection.

The final source guard parses products.py and rejects eight named import roots regardless of their local import alias. It does not trace dynamic imports/calls, subsequent name aliasing or transitive dependencies. Provider authority, authorization, operational success, exhaustive type/limit matrices and all node-control surfaces remain outside this file's evidence.

Read depth: full 755-line test and local hostile fixtures; actual runtime-contract constructor/descriptor/codec and selected socket, port, mount, lifecycle, verification and instantiation helpers; actual algebra socket definitions, selected environment/configuration/verification/lifecycle and graph-finding owners. Previously reviewed image/document context was retained. No full products.py or all imported-language audit, executed tests, monkeypatching of a running application, database or provider actions were performed.
