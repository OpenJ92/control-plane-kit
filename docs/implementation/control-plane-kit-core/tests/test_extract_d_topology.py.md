Source: [control-plane-kit-core/tests/test_extract_d_topology.py](../../../../control-plane-kit-core/tests/test_extract_d_topology.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

This file protects the recorded Extract D decomposition, its child-issue
classifications and one-way server-to-Core boundary in
[EXTRACT_D_TOPOLOGY.md](../../../../control-plane-kit-core/docs/EXTRACT_D_TOPOLOGY.md).
It checks exact historical phrases/rows, not the execution status of those
GitHub issues or the correctness of current server implementations.

The enduring ownership statement is that Core does not package/import the
canonical cpk-server process. Historical phase numbering and issue prose are
not a new runtime contract; deliberate historical-document edits must account
for these existing text assertions.
