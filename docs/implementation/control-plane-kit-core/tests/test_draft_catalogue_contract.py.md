Source: [control-plane-kit-core/tests/test_draft_catalogue_contract.py](../../../../control-plane-kit-core/tests/test_draft_catalogue_contract.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

Two tests check catalogue declarations without reproducing graph or durable catalogue laws. Create/revise must have exact workspace POST paths, PLAN_WRITE scope, COMMAND safety, positive request/response byte bounds, named MCP bindings, required idempotency and no separate approval requirement. The three reads must have exact GET paths, READ scope, READ_ONLY safety, positive response bounds and named MCP bindings. Draft summaries and revision histories are paged with a 100-item declaration; revision detail is unpaged with no page maximum.

The imported [_uow fixture](../../../../control-plane-kit-core/tests/test_command_parity_contract.py) builds pure service/transaction boundary values, not a database transaction. Selected actual [HTTP declarations](../../../../control-plane-kit-core/src/control_plane_kit_core/operations/http.py), [parity factories](../../../../control-plane-kit-core/src/control_plane_kit_core/operations/parity.py) and the [projection owner](../src/control_plane_kit_core/operations/projections.py.md) supply this composition. Factory/constructor validation participates, but these methods add only positive assertions. Their route dictionaries do not independently establish uniqueness.

Positive byte bounds do not pin exact maxima or prove payload enforcement. The tests do not directly assert workspace flags, schema names, service roles, projection redaction policies or MCP read-only annotations, and do not exercise authentication, idempotent persistence, cursor traversal, approval handling or external effects. NOT_REQUIRED describes these catalogue command contracts; it does not authorize deployment or destructive provider actions.

Full 60-line test read with the actual _program/_transaction_rule/_uow helpers and selected exact HTTP/parity/projection entries; prior full projection/MCP/transaction context retained. No tests, imports, database or provider actions were run. Operations tests own the concrete catalogue workflow.
