Source: [control-plane-kit-operations/tests/test_gateway_security_read_projection.py](../../../../control-plane-kit-operations/tests/test_gateway_security_read_projection.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

Fake probe/key stores and a recording workspace capability prove independent store use, lookup ordering, attempt projection and public verifier/key-inventory shapes. A forbidden store ensures missing workspace prevents capability access. Missing and foreign probe details share a cause-free public error; verifier failures retain their selected wrapping, and a verification set without an active member is refused.

Inventory omits private-key references; verifier output intentionally includes public PEM and its environment representation. Synthetic key text is a projection fixture, not proof of valid Ed25519 material, possession, signing, installed verifier state or replay resistance. No gateway/provider is contacted. The fake key store is not a concurrency or active-key uniqueness proof of the Postgres owner.

Malformed inventory is categorically rejected, while unexpected probe descriptor failure and missing workspace attributes remain raw seams. Tests do not claim universal sanitization. Existing structural tests check private ownership, selected dependency forms and facade signatures/delegation; a spy checks exact argument/result identity. Keep those historical extraction checks distinct from security behavior or new source-layout policing.

Full test source read, including fake helpers and structural checks; imported helper behavior was checked with the preceding package-foundations group, not re-executed. See [gateway_security.py](../../../../control-plane-kit-operations/src/control_plane_kit_operations/read_services/gateway_security.py), [gateway_probes.py](../../../../control-plane-kit-operations/src/control_plane_kit_operations/gateway_probes.py) and the [key store](../../../../control-plane-kit-operations/src/control_plane_kit_operations/postgres/delegation_signing_key_store.py) for actual value, transaction and selection laws.
