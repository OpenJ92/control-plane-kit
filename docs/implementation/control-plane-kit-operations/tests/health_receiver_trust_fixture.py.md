Source: [health_receiver_trust_fixture.py](../../../../control-plane-kit-operations/tests/health_receiver_trust_fixture.py).

This test-only format witnesses the Operations decoder port. It reads actual
selected artifact bytes and returns configured family facts. Expected graph
identities are never copied from the selection to manufacture agreement.
It is not the CPK or gateway product parser and earns no Servers208 evidence.

The fixture starts with the existing valid management topology, attaches exact
test product references and selected artifacts, then compiles the real health
plan. Product descriptors deliberately trust a different default key; a passing
selected-key case therefore cannot result from descriptor-default fallback.
Registered products use the existing registration service. Graph, preparation,
first-start and reload state machines are not implemented here.

The decoder performs no signing, effects, state transition or private-key work.
Public key fixtures and malformed-input witnesses are synthetic. Operations
tests use Core public identity semantics; cryptographic receiver parsing remains
product-owned and was independently accepted in Servers207.

Target-only checkpoint for1857: static review and native red remain pending.
