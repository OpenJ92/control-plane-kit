Source: [test_postgres_health_receiver_trust.py](../../../../control-plane-kit-operations/tests/test_postgres_health_receiver_trust.py).

Eight strengthened/new-law targets call the existing real PostgreSQL first-start
and reload services. The existing fixture creates the approved ready plan and
active key/reference/provider owners. This subclass supplies registered test
products and selected files before plan compilation, then checks the retained
plan/registrations before the1857 missing-module guard.

The behavioral witnesses are: actual selected bytes versus deliberately wrong
descriptor defaults; new valid artifact digest with wrong material versus
overlap; independently configured identity/issuer/purpose/key/declaration
mismatches; missing/unsupported bindings and reassigned slots; exact original
base-side pins on repeated reload; missing reload support with both replay
entrances still observation-only; bounded contract refusal versus unexpected
decoder/store error identity; and reload coverage refusal without history repair.
Both replay entrances also run with the registry argument omitted, independently
of the populated-registry decoder trap. This protects historical observation
when no receiver decoder is configured, rather than merely proving no decode.

Refusal checks observe zero ID allocations and unchanged durable owner facts.
Successful starts, preparations, reloads and replay use actual existing services;
the adapter does not simulate their state machines. Fixed injected database
observation stays inside the original grant interval so wrong-time failures do
not masquerade as coverage failures. Existing predecessor tests retain rollback,
concurrency, commit-then-error and interval-edge evidence.

This target-only slice leaves predecessor fixtures/assertions unchanged so the
initial missing-contract evidence is focused. During the subsequent approved
implementation slice, shared first-start/reload fixture construction must adopt
the same explicit admitted products/artifacts/registry; their existing behavior
assertions must be preserved. Existing exact constructor-signature assertions
must reflect the approved additive keyword. That migration is fixture/API
alignment, not a fallback path in application code. Static reviewers must assess
this plan before native red; no source or suite has run at this checkpoint.
