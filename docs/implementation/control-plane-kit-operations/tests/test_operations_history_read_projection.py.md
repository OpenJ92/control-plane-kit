Source: [control-plane-kit-operations/tests/test_operations_history_read_projection.py](../../../../control-plane-kit-operations/tests/test_operations_history_read_projection.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

Seven methods cover the Operations-history extraction boundary and selected recovery-description failures. Four inspect source structure; three invoke the actual facade with in-memory workspace, graph and activity-store doubles. Typed records and Core graph values supply the fixture; RuntimeKind.DOCKER names an authored graph runtime and does not contact Docker. There is no database or external provider fixture.

The structural group locks eleven facade methods and their moved helper family to the private operations_history owner. It checks exact method/function sets, one-return forwarding through _operations_history, removal of moved dependency attributes from the facade and absence of imports back to instance/workspace_graph. Five small parsed import examples exercise the imported _local_module_imports helper's supported relative/absolute forms. These are intentional AST extraction contracts, not exhaustive dynamic-import detection, argument-flow proof or behavioral parity for every listed method.

The behavioral matrix runs six cases through each of plan_detail and approval_detail: missing base, missing desired graph, foreign workspace, rejected decoding, a validation-time TypeError and an invalid graph containing a missing child. Each requires exactly ReadModelError with fixed unavailable/invalid text, repr length at most 96, no selected hostile coordinate/candidate and no cause/context. The row named transition supplies an invalid decoded graph; it does not independently prove a fault inside the later transition planner. A separate plan-detail test verifies that an unexpected driver RuntimeError escapes as the same object rather than being relabeled or sanitized.

The production owner computes a pure desired-to-base recovery description through Core validation/planning; these tests do not execute recovery, approve destructive action, contact providers or establish that compensation succeeds. Catching BaseException inside the assertion matrix records an outcome for an exact-type check, not a rule that production should swallow cancellation. Fixed expected errors do not imply universal redaction of all exception types.

Full 471-line test owner and actual imported _local_module_imports helper read with previously fully read [Operations history](../src/control_plane_kit_operations/read_services/operations_history.py.md) and [facade](../src/control_plane_kit_operations/read_services/instance.py.md); the current recovery method and approval call path were rechecked. No executable validation or full transitive Core audit. Use the established Docker-backed Operations suite for execution; the note does not introduce a host-local test path.
