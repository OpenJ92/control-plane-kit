Source: [control-plane-kit-operations/tests/test_read_services_documentation.py](../../../../control-plane-kit-operations/tests/test_read_services_documentation.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

This four-method owner is a documentation contract for two structured text blocks in [READ_INTERFACES.md](../../../../docs/READ_INTERFACES.md), not a runtime authorization, schema or projection test. The default path is the repository document; CPK_READ_INTERFACES_DOCUMENT can select another file at module load. The class reads UTF-8 text once. It neither discovers production routes nor queries stores, databases or providers.

The required ownership block names the original five projection families between Postgres stores and InstanceReadService, followed by the shared server application and HTTP/MCP surfaces. Its exact equality does not prove that it exhaustively lists every current projection family. The public-material block distinguishes bounded signing-key inventory metadata from purpose-limited public verifier PEM/environment; it forbids private keys/references, provider credentials and resolved secret values as a documentation claim. Source-level enforcement must be verified by the relevant projection/security owners, not inferred from this text test.

Both positive methods call the same validator, so either also fails when the other required block changes. _single_text_block requires exactly one matching second-level heading and one closed text-fence block within its section. Negative methods duplicate each declaration/block and append each of three obsolete ownership/disclosure strings, expecting rejection. This is exact string/fence matching, not a general Markdown parser, semantic contradiction detector or arbitrary secret scanner.

Full 120-line owner read and both governed document sections checked. Assertions describe the existing literal contract; they do not authorize copying its five-family list as a complete current architecture inventory. No tests were run. Use the established Docker-backed Operations suite for executable validation, even though this owner itself only reads text and needs no service fixture.
