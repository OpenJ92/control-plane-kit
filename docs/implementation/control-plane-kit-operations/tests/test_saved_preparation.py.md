Source: [control-plane-kit-operations/tests/test_saved_preparation.py](../../../../control-plane-kit-operations/tests/test_saved_preparation.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

Fifteen real-store methods protect exact saved-revision preparation, durable replay and staged transaction behavior. The [fixture](saved_preparation_fixture.py.md) supplies a unique schema in the trusted package database, actual services and deliberate exception/clock/SQL-observation seams. These tests mutate records, corrupt descriptors and exercise concurrency; they are not read-only diagnostics for retained data. Execute only through the established Docker-backed Operations suite.

The accumulated-edit scenario retains several immutable revisions, selects the final intended graph, inspects generated StartNode targets and one correlated approval request, then edits/selects again. It distinguishes an old completed replay from a new stale-key rejection and a fresh preparation. Current lineage and selected runtime/effect tables remain unchanged, with no approval decisions, runs, effects, events or observations created. This is planning/approval-request truth, not deployed nodes or provider convergence. Preparing an old but still selected revision is valid even when the draft head is newer.

First-admission negatives fence current/desired authored and projection IDs, desired generation, revision and workspace before observed mutation statements. Invalid stored graphs, unsatisfied graph requirements and revoked products reject. Replay tests intentionally permit later selection/head/session/product drift after completed preparation but reject changed parent intent, false session/start-action evidence, malformed saved commitment/revision/projection facts and corrupt retained current projection. Historical replay is not permission to execute a stale plan. Fixed selected error canaries are checked, not every exception's redaction.

StopBefore/StopAfter interrupt after admission, planning or approval-request commits and a newly constructed program resumes without duplicating the compared plan/approval truth. No operating-system or database process restarts occur despite restart/physical wording. A separate connection observes no committed session writes when start_in_unit_of_work is called without commit; a late action-ID uniqueness conflict rolls back the attempted transaction. Snapshot comparisons cover the fixture's listed tables, not every database object; the separate source-association suite covers that relation explicitly.

Two identical callers synchronize before preparation and converge on one plan/request. Other two-thread cases hold admission inside its transaction, observe a competing workspace-lock attempt and verify selection/tombstone waits until release. The reverse order deletes an unused draft before attempted admission, which then fails without a session. These are controlled overlap witnesses, not exhaustive schedules. Barriers/events, selected result waits and UoW SQL have explicit timeouts, but executor.map/context shutdown do not provide a universal whole-test deadline.

The direct admission-owner case verifies it computes canonical metadata without receiving a caller digest and replays stored session truth. Expected metadata is independently constructed by the fixture. Full 459-line test and 120-line fixture read with the full admission helper, previously read association tests and selected actual parent preparation/session/UoW/lock contracts and ancestor observers. No tests or external actions were run; no general recovery, tamper-proof history or whole-store audit claim.
