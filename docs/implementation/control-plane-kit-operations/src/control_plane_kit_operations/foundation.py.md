Source: [control-plane-kit-operations/src/control_plane_kit_operations/foundation.py](../../../../../control-plane-kit-operations/src/control_plane_kit_operations/foundation.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

OperationsPackageBoundary is a frozen descriptive value exported through the package root, not runtime dependency injection, authorization or an executable architecture checker. The constant names the distribution/import package, its Core package edge, the current Core DeploymentProgramStage enumeration, intended owner labels and excluded server/framework/provider owners. Constructing its descriptor turns tuples into lists and stages into their string values; there is no constructor validation or deep schema enforcement.

The future_owners field and “before service code arrives” wording are historical scaffold vocabulary. Many listed owners already exist. They are not a current completion ledger, permission to duplicate a class, or a requirement to preserve obsolete module names. Likewise depends_on describes the architectural Core edge, not every installed Python dependency: pyproject also declares psycopg. deployment_spine reflects the imported enum in iteration order, not proof that every stage is implemented or successfully executed.

Selected package-boundary tests separately inspect pyproject dependencies/no entrypoints, compare the declared spine with Core, and check selected owner/exclusion labels. Separate import scans police package directions; this data object itself cannot prevent forbidden imports. The value has no provider/network/database behavior or secret inputs. Callers constructing arbitrary alternate values do not receive validated contracts merely because the dataclass is frozen.

Full56-line owner read; actual root export and selected test_package_boundary metadata/descriptor/import assertions checked. No full import-scan implementation or packaging audit was performed for this note, and no imports/tests were executed. Use current source, package AGENTS.md and dependency contracts—not the historical future label—as implementation authority.
