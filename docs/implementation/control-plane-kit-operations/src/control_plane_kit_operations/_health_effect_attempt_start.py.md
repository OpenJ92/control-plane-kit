Source: [_health_effect_attempt_start.py](../../../../../control-plane-kit-operations/src/control_plane_kit_operations/_health_effect_attempt_start.py).
Maintain this companion alongside its source.

This private module supports the existing first-start owner. It cannot open or
commit a unit of work, resolve material, sign or dispatch. Its closed admission
and write values carry only symbolic references and unsigned Core health values.

Fresh admission joins the actual approved request/decision and planned ready
activity, rederives scope/destructive/risk requirements, loads both explicit
realized projections and their authored records, and uses the accepted pinned
health projection. It preserves selected base/desired authored identity even for
equal graph content. Active key selection uses the two dedicated health purposes
under their existing shared locks. Registration identity, public material and
private references must agree, and the two families must be distinct.

The shared receiver-coverage check then selects each exact registered product
and declared artifact slot from the approved graph side. Trusted decoders read
the actual selected bytes; each receiver must cover its active signer's full
public identity and configured context. Missing composition refuses before
correlation locks, time, IDs or writes. Historical replay never decodes trust.

Both actor-bound deterministic correlations are locked and required absent
before either authorization. The worker fence is not secret-use authority.
Only the existing database lease observation supplies time: integer ceiling for
the start of validity, the minimum of the lease floor and both family bounds for
expiry. A nonpositive interval refuses before IDs or writes. No fresh clock or
deadline is invented. Request/JTI values are built and checked before writing.

After the owner writes event, intent and attempt, each existing reference-use
authorization helper runs in the same unit of work. Its `(use, provider)` result
is checked against the exact admitted reference and a pure expected candidate,
then against a retained use readback. The preparation's insert acknowledgement
must equal the exact nominal candidate; the complete result is validated before
the owner requests commit. Mixed store/helper exceptions retain identity; new
pure checks and acknowledgement mismatches use bounded errors. Arbitrary owner
exception text is not a public error projection.

Replay reads #1851's retained preparation and original owner joins. Dedicated
replay also verifies both original use actors against the current context. It
never selects current keys, samples time, authorizes, renews, repairs or writes.
Missing/corrupt retained evidence refuses. PostgreSQL targets protect approval,
pins, active selection, correlation absence, interval bounds, acknowledgements,
rollback, real commit-then-error and serialized concurrent starts. Source-boundary
tests exclude provider/signing/dispatch/clock/commit calls in both health modules.
