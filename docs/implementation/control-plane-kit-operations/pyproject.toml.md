Source: [control-plane-kit-operations/pyproject.toml](../../../control-plane-kit-operations/pyproject.toml).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

This is the Operations distribution's setuptools build/installation metadata, not a deployment or runtime authority manifest. It declares version0.1.0, Python>=3.11, setuptools>=68/build_meta, Core>=0.1.0 and psycopg[binary]>=3.2. These are lower bounds, not a lockfile, exact Core Git pin or reproducible dependency closure. Pre-Alpha classifiers and the homepage describe the package; they do not certify release readiness or provider support.

Package discovery is rooted at src and includes control_plane_kit_operations*. The declared package-data entry carries postgres/current_schema.sql with the installed package. No console scripts or optional dependency groups are declared. Operations does not become a server/process or concrete provider interpreter through installation. The psycopg binary extra requests the packaged driver option; installation may resolve/download dependencies in the authorized container environment.

The [ordinary package gate](test.sh.md) copies same-checkout Core and Operations into ephemeral container paths and installs Core first, then Operations. It therefore exercises that source pair, not every version permitted by these lower bounds or a published-wheel-only consumer. It defaults to Python3.14, which does not prove the whole declared Python>=3.11 range. Its final clean-container import checks version0.1.0 and absence of architecture-testing, not a complete wheel-content/dependency audit.

Selected package-boundary tests assert exact distribution/dependency fields and absence of optional dependencies/scripts. The descriptive [foundation](src/control_plane_kit_operations/foundation.py.md) records only the architectural Core edge, not the full dependency list here. Full35-line metadata and full132-line gate read; selected actual package-boundary assertions, root exports and CI Operations job checked. No build, install, import, test or host-runtime action occurred; no dependency or packaging behavior changed.
