# Operations implementation companions

This slice follows the [CPK #1799 convention](https://github.com/OpenJ92/control-plane-kit/issues/1799). Append the repository-relative source path with its original suffix to `docs/implementation/`, then append `.md`. Each note starts with its source link and same-change reminder. Read it with actual owner source and selected contract-bearing dependencies, never instead of them.

The [initial inventory](inventory.json) covers 310 tracked implementation, test, schema and meaningful process/build inputs: 119 reviewed companions, 188 pending entries and three exclusions. Existing guidance is maintained directly rather than recursively mirrored. The reviewed notes cover packaging/test-gate inputs and package-boundary tests, gateway-key rotation language/storage/tests and overlap publication/preparation/execution/fixture/tests and rotation activation/tests and retirement publication/fixture/preparation/tests, paging contracts/tests and current-metadata and ordinal page tests, read-cardinality policy and large-route parity and large-collection paging tests/shared fixture, package foundation/workspace creation, attempt-intent admission/history, runtime translation/bootstrap, execution/rotation-child admission, approval requests/decisions, product registration/storage, image-pull, ingress and runtime/delivery authority stores/values/tests, secret-provider/reference/use admission and storage/tests, delegation-key generation/registration, gateway-probe receipts/storage and their tests, read-service foundations/facade, workspace/graph, operator overview and authority/secret/gateway projections, selected projection/documentation tests, observation freshness, canonical temporal codecs and ordering/page tests, saved-preparation admission/association storage, governing/value/adapter tests and draft fixtures, desired-draft catalogue commands/store/selection/adapter tests, authored graph and desired realized-projection publication, workspace/graph/projection stores and their tests, and draft/revision-history reads, attribution, storage, fixtures and tests; the PR records independent review depth. Pending means rollout work, not certified absence of defects or a reason to block unrelated work.

Keep maintenance in the existing PR flow: inspect the actual source/dependency diff, pair relevant creates/moves/removals, and update affected meaning. If unchanged, record “companion reviewed; no semantic update needed” in the normal decision log. Bring newly touched pending files current. Verify selected dependency versions and coordinate actual adoption across repositories; reverse links are not exhaustive.

No per-file timestamp/hash churn, extra report or new validation programme is required. Review consequential claims against source. Documentation coverage does not establish provider outcomes, resolve uncertainty, or grant authority to execute effects.
