Source: [control-plane-kit-operations/session-command-serialization.json](../../../control-plane-kit-operations/session-command-serialization.json).
Maintain this document alongside its source file. When a mapped command, lock boundary, replay rule or transaction owner changes, verify and update this companion in the same change.

This version-1 contract maps 21 command names to session-action writers and their intended serialization roles. It is review/test data, not an executable locking coordinator, runtime command registry, authorization policy or migration. Its scope is durable commands appending cpk_operation_actions, not every Operations write or external effect. No tests or database actions were run for this note.

The declared order is command-identity, session-lifecycle, workspace-graph, dependent-truth, then action-ordinal. Commands use an ordered subset: graph-independent approval/lifecycle commands omit the workspace role, while graph commands include it. Identity is serialized before reading replay evidence; a first nonterminal command must re-read OPEN under the session row lock. Dependent rows follow the applicable session/workspace roles, and the application owns the transaction containing domain truth and the appended action. This role order does not enumerate every lock within a role or prove freedom from all deadlocks.

Actual [history-store helpers](../../../control-plane-kit-operations/src/control_plane_kit_operations/postgres/activity_history.py) use transaction-scoped advisory locks over named workspace/start or session/action key strings, session SELECT FOR UPDATE and a locked MAX(ordinal)+1 allocation. The manifest does not itself acquire those locks, enforce payload fingerprints, authenticate actors or redact actions. Command services and their stores remain authoritative for those behaviors.

Session start is an explicit pre-row exception: a workspace-scoped start key is locked before the session exists, and the newly inserted first action uses ordinal 1. The ordinary workflow owns its commit. Saved preparation holds the same start key, then workspace/draft truth before calling the non-committing start writer, and stores the relational source in its owning UoW. The helper does not commit for its caller. Some lifecycle commands use immutable request/run linkage to locate the session before taking its lock; that is not a substitute for authoritative lifecycle admission afterward.

Six outcome descriptions cover identical replay, changed-intent conflict, competing pointer commands, terminal fencing, terminal replay and transaction atomicity. Treat them as intended semantic constraints, not a universal byte-identical response theorem. For example, the [workflow implementation](../../../control-plane-kit-operations/src/control_plane_kit_operations/workflows.py) returns an existing durable action on terminal/record replay while reading the current session and marking replayed=True. Start replay likewise returns the persisted session/action, not a frozen original response envelope. Fresh admission, replay evidence and current projections must be distinguished in each owner's tests. The manifest grants no retry authority for ambiguous external mutations and promises no cross-transaction/provider atomicity.

The sole executable consumer found in current package source is [test_session_command_serialization_contract](../../../control-plane-kit-operations/tests/test_session_command_serialization_contract.py). Its seven methods check the five ordered role names/ordinals, literal command-name set, required nonempty descriptions, writer-set coverage, ordered role subsequences and six named outcome laws. Schema_version/contract_id and every prose statement are not independently semantically validated by these checks.

Writer coverage comes from an AST walk finding ordinary function bodies with calls spelled add_action, reducing several recognized session-helper names to get_session_for_update. Source-ordered call names must place identity before session before action allocation; recovery/retry paths add selected lease-observation/event-ordinal ordering. Start is skipped by that generic call-order check. A separate textual check orders the rotation locator, workspace lock and rotation lock. These witnesses do not evaluate branches, resolve arbitrary receiver/alias targets, discover all alternate write syntax or establish actual runtime lock acquisition, isolation, rollback, authorization or concurrent outcomes. A writer may represent multiple command names, such as close/cancel, so 21 mappings are not 21 distinct implementations.

When a command changes, inspect the real transaction and replay path first, then update its service/writer/source/role mapping and owning behavioral tests. Keep the manifest descriptive; do not add a second workflow or recovery state machine to make it authoritative. Existing recovery/compensation entries describe committed owners and do not revive deferred product requirements or authorize execution. Public adapter auth and provider authority remain outside this file.

Read depth: full 215-line manifest and full 212-line consuming test/helpers; selected actual workflows206–364, history advisory/session/ordinal helpers and saved-preparation105–176, plus retained planning/admission/rotation context. Not a fresh full audit of every mapped writer or all concurrency tests. Documentation/source guard and links only; no executable validation, source/schema change or new security/mutation surface.
