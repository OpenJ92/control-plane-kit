Source: [.gitignore](../../.gitignore).
Maintain this document alongside its source file. When the source or relevant
imported contracts change, verify and update this companion in the same change.

This file keeps ordinary Python caches, local virtual environments, build and
distribution output, package metadata and macOS Finder metadata out of routine
untracked-file reporting. It does not remove already tracked files, sanitize
artifacts, or protect credentials. In particular, no general secret-file or
generated-report exclusion is declared here. Inspect actual Git status and
staged paths before publishing; never treat ignore matching as an authorization
or confidentiality boundary. Relevant authored source remains in the companion
inventory regardless of incidental ignored local files.
